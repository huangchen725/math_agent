# 安全说明

## 支持范围

当前安全维护范围是 `main` 分支上的唯一运行实现 `math_agent/`，以及竞赛兼容入口 `user_agent.py`、批处理入口 `main.py` 和 Demo 入口 `demo.py`。根兼容入口不得复制或分叉包内实现。

## 报告问题

请通过代码托管平台提供的私密联系渠道向仓库维护者报告漏洞。报告中应包含受影响文件、复现条件、潜在影响和建议修复；不要在公开 issue 中粘贴 token、私有题目、完整模型响应或可直接滥用的凭据。

## 信任边界

以下内容均视为不可信输入：

- JSONL 题目、`idx` 和 metadata；
- 模型返回的文本、`tool_calls`、函数名和参数；
- 自定义 API 地址返回的 HTTP/JSON 内容；
- 从外部来源下载的依赖、数据集和提交压缩包。

## 必须保持的控制

- API key 只从环境变量读取，`.env` 必须被 Git 忽略。
- 输出文件名只能由受限的安全索引生成，不能接受路径分隔符或 `..`。
- SymPy 表达式只能在无 builtins 的受限命名空间解析，并限制字符、长度、指数、矩阵维度、整数位数、单轮工具数和结果大小；每次工具计算必须在可终止子进程中执行并受墙钟超时约束。
- 每题必须共享模型请求、已报告 token、工具调用和 deadline 预算；题目与 metadata 在模型调用前执行类型、序列化和长度检查。
- HTTP 仅重试连接、超时、限流和服务端错误；HTTP 400 只有在受限响应字段明确表示频率限制时才可重试，认证与普通请求校验错误不得盲目重试。
- 默认测试不得访问真实模型 API。
- `outputs/` 可能包含题面、推理和工具结果，只能作为本地敏感运行产物处理。

## 已知限制

- 数学表达式的复杂度无法仅通过字符长度完全界定。子进程硬超时能终止长时间计算，但尚未设置操作系统级内存上限；短时间内的内存峰值仍需后续防护。
- LLM 可能受题面中的提示注入影响。工具执行层只允许注册函数，但模型输出与 trace 仍可能包含攻击者提供的文本。
- 单题 deadline 在模型/工具调用边界检查，不能提前中断一个已经发出的阻塞 HTTP 请求；单请求仍由客户端自身超时保护。
- token 预算依据服务端返回的 usage 累计；若一次响应使总量越界，系统会阻止后续调用，但无法撤销已经产生的 token。
