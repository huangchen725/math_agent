# 首日扩库与快答实施、诊断及验收（2026-09-10）

本轮按用户“首日全部优化、今晚零点前完成”执行，19:40开始。用户随后明确不处理GitCode。本报告区分工程交付、原始在线诊断和最终离线回放；没有触发正式评测，不承诺35%或绝对零风险。工程起点为43a6a1478c300ef1f94d68915a8bc9bb47333179；已正式运行的兼容回退点仍为b87e4b9。

## 1. 最终交付

- 1054条有完整题面、原答案、来源版本和许可的公开题答：U-MATH606、Diffy Qs222、DMOI第三版226。MIT606、CC BY-SA4.0共448；完整署名与版权见根THIRD_PARTY_NOTICES及资源附属文件。
- 分桶索引、代码固定摘要、惰性有界读取；完整题面匹配保留数字、符号、否定、数域、边界和全部小问。构造不读取全库，无向量服务、网络下载或新增正式依赖。
- 同题一次实质模型核对快答；必须同时有明确条件核对、核算过程、YES/A、完整终答及严格答案一致性。本地数学fail否决，unknown不冒充证明；核对拒绝/截断时回到同题剩余预算，不重启整轮。
- 完整source answer_body只剥离锚定英文答案标签和折叠排版空白；保留编号、顺序、条件、角度、下标与TeX行分隔。说明性长解不做快答。模型核对通过后采用给定答案格式，避免数学正确却因翻译改写被拒绝。
- CHECK核算允许有界换行：至多16个非空行、合并CHECK至多2000字符、总响应6000字符；末三字段仍严格，重复/隐藏标签、unknown、length均拒绝，交付保留全部核算续行。
- 方法参考最多给一个普通候选。独立概念、明确共同方法或非平凡完整数学片段不足时不给参考；同义词展开不能充当多个独立命中，函数/分数幂结构冲突直接拒绝。
- 有限正确答案交付修复：只在明确stop、唯一完整英文Final Answer数学块且整题精确核验pass时补规范末行。真实历史响应0014已回放验证，未放宽length、空最后标记、冲突答案和工具JSON边界。

正常ReasoningAgent(client)默认明确开启answer_bank_fastpath、answer_bank_reference、completed_answer_repair，并保留43的四项已启用能力。显式Q1Policy()仍全关闭。旧corpus_retrieval、其余实验开关未自动加入。输入/配置不能从metadata解锁新能力。

最终代码摘要：

- user_agent.py：d716a906a3362cdbdf5e438a593062aaa3f838578c82996efb2efaa8d9da3158
- xh202627_corpus.py：b5f56ee6745bedbb4d13ff7cf7004023b410303c04323150f1e51f860ec6c983
- 题库根manifest：4ed168f2a36c48d314ddc33c37b64aa7c71eafc5a1489598553154d8adaa93b1

## 2. 数据质量与规模边界

Q0原144题及数字掩码近重复排除；U-MATH共排除290条重合/近重复，另隔离3条公式控制字符损坏。教材一条说明矛盾被隔离。固定命中探针独立审核发现一条面积答案缺参数符号条件，再整条隔离；不改源答案，不更换探针。

1054条逐字段与固定原源重构一致，1054次完整记录往返、508索引和4记录块摘要全部通过，独立重建根摘要一致。最终896条满足快答预筛、158条仅参考；这是格式/来源资格数，不是正确数。896条均不能由现有确定性核验器证明，必须经过模型实质核对。原991的中间资格统计保留于旧报告，不混为最终数。

按10万条测试容量，不把合成容量写成真实入库数。三个独立进程的逐题读取及内存受控；没有依赖跨题题答缓存。最终资源探针回执保存于outputs/day1-20260910。冷启动指独立解释器，不声称清空操作系统文件缓存。

## 3. 真实API结果（全部已结束）

模型回执为Intern-S2-Preview-397B，请求始终携带thinking_mode=False。原批次冻结六道未入库公开题，使用43完整源码作对照；另保留八个固定库内探针。其后仅为明确格式缺陷追加同八探针确认，不重新抽题或重跑六题配对。

| 批次 | 实际请求 | 结果边界 |
| --- | ---: | --- |
| 43基线六题完整流程 | 38 | 独立完整题意审核5/6；另1题只在正分支成立，原参考自身也有分支不完整问题 |
| 初版候选同六题 | 37 | 4/6；有正确候选但选择错误1题，无净增益 |
| 初版固定8命中探针 | 8 | 1次成功快答；7个合格源的核算多数正确，主要因答案改写被严格键拒绝；坏源探针保留 |
| 格式修复后同8探针确认 | 8 | 3次真实快答成功；5条满足修复后资格，另2条说明文和1条坏源退出快答；一个正确源遭模型错误B，另一个正确核算因CHECK换行被拒绝 |
| 合计 | **91** | **139364实际token**（输入68391、输出70973）；请求输出额度464384；无重试、无未知用量 |

所有批次共同受原200请求、650000请求输出额度、19:49:04开始至22:49:04的三小时绝对窗口约束；确认没有重新获得三小时。最后真实调用在20:28前完成。货币费用未取得，不编造。

单调用探针在尝试第二个请求前主动结束。它的“退出快答/未完成完整solve”不能统计为正式invalid或整题错误。

## 4. 发现的问题与最终处理

1. **弱参考匹配。** “积分”经同义展开被错误计算为多个相关概念，曾推荐另一种不同积分的参考。已收紧资格；六道诊断题参考4→0，72开发题46→0。未读标签、未按题号特判，这是参考资格变化，不是准确率变化。符合明确共同方法的参考仍有覆盖测试。
2. **原有选答盲点。** 一道三重积分的新旧四个生成请求完整参数相同，未注入参考，未触发新交付修复；新池有正确答案，但三个验证器全A，原平分规则选了错误候选。它不能归因于新参考，也不能宣称已通过关参考修复；这是次日计算核验/选答的明确工作依据。
3. **格式性快答损失。** 保留严格键，提供answer_body，说明文降级；最后仅修CHECK换行。原八条确认请求在新旧解析器上四参数完全相同，0新API回放接受数3→4，只有换行题改变；错误B仍拒绝。这不是第四次真实实验或新正确率。
4. **全量门禁超时。** 原两次formal分别超过60秒；无门限定位为1052项通过但64.38秒。热点是Q0重复题相似度校验。改为单次verify_bundle内部复用字符Counter，只预筛不可能达到原0.88的题对；原最终双向判断、文件重读与hash不变。独立961对穷举和性质测试一致，未删测试或延长门禁，旧失败记录保留。
5. **离线诊断准备异常。** 审查补齐原8探针绑定及跨批累计时间/费用；结束后进一步在工作线程发生准备异常时立即停派发，避免按future顺序等待造成延迟。实际91请求没有该准备故障。原执行工具按原hash另存frozen-diagnostic-tools-v1，旧账本和raw不改，修补后的工具不冒充原执行版本。

## 5. 最终验收与交付凭据

最终未命中路线在43的38条预录请求上逐条比较全部messages、temperature、max_tokens、thinking_mode后才释放响应；6份final_response UTF-8字节一致，trace仅排除耗时字段后相同。它证明这些固定响应下的行为回归，不是最终版本重新在线得到5/6。

两种Python最终完整formal、Ruff、编译及实际提交包验证的具体时间、文件摘要和提交号记录在本节末尾的完成回执。60秒上限保留。双解释器隔离发布探针8/8通过：完整真实库1次请求、缺库/坏manifest/坏记录各6次原流程；仓外cwd、-I、15个同名模块污染、严格四参数与私有属性陷阱、metadata/trace隔离均通过。进行了Linux路径大小写审计，没有冒称真实Linux容器或正式平台已运行。

主要本地证据：

- outputs/day1-20260910/full-pair-review.json
- outputs/day1-20260910/hit-math-review.json
- outputs/day1-20260910/confirmation-parser-replay.json
- outputs/day1-20260910/reference-gate-audit.json
- outputs/day1-20260910/data-release-audit-final.json
- outputs/day1-20260910/release-probe/report-release.json
- outputs/day1-20260910/replay-final/report-release.json
- outputs/day1-20260910/validation-py312-release.json
- outputs/day1-20260910/validation-py310-release.json
- outputs/day1-20260910/release-static-audit.json

工程交付不等于正式提分：可靠同题具备一次核对结束的证据；未入库推理正确率没有提升证据，错误认可和选答仍是下一日重点。英文整题严格身份不自动覆盖中文翻译或参数变题，真实隐藏题命中率未知。GitCode按用户要求未操作，本轮未触发正式测试。

### 完成回执

最终源码两种Python均1114项完整测试通过：3.12.14测试42.96秒（formal CLI 44.02秒），3.10.11测试44.90秒（CLI45.72秒）；81个Python文件编译及Ruff通过。验证前后全部Python/资源摘要一致。原60秒超时记录保留，已通过保持判断与重读不变的离线性能修复关闭本地阻断。

最终b5f56语料读取在既有10万合成库的3独立并发进程中再次验证，各20精确命中+20改条件拒绝；每题最多162104字节、8条正文，P95不超过11.31ms、额外峰值RSS不超过942080字节。记录在report-release-capacity.json；它不是正式题命中率。

正式配置/四参数_chat/私有加载器AST与43完全一致，依赖输入和其余四个正式源未改；可版本化文件未发现实际密钥。提交及Git对象导出包的最终摘要见outputs/day1-20260910/commit-receipt.json和package-receipt.json。本报告与代码一同本地提交，GitCode不操作，GitHub本轮未推送。
