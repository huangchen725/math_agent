# B1/B2 提示一致性离线工程

> **后续实测更新（2026-09-08）**：下方保存原离线阶段记录。用户随后批准的小批真实验证已完成：B1 为 4 题/8 请求，单阶段正确交付 4→3；B2 为 8 题/16 请求，2→3。均发送 `thinking_mode=False`，默认开关仍关闭；结果不足以声称稳定或完整流程提分。最新回执、独立计算和最终两版本 773 项门禁见 [第一批验收](FIRST_BATCH_20260908.md)。

基线：`9acff7e`（正式求解行为与 `5c2f7a0` 相同）。用户授权 B1/B2 分别实施、分别提交；工作分支 `work/b1-b2-offline-20260908`。本轮零真实模型请求，不推送或发起正式提交；旧 v3 实测维持暂停。实验开关默认关闭，工程完成不代表提分。

## B1：提示匹配真实工具能力

- 独立开关/计划名：`tool_aware_prompts`。
- 仅替换固定领域提示的工具指导和无适配器工具来源请求中的调用工具要求。数学策略、易错点、公式和 few-shot 不变；不重写题面或模型输出。
- 普通候选与多样性候选均使用文本提示，包含显式适配器下的普通阶段；实际 `run_tools` 路径仍保留原消息。
- 不变：三候选、温度、8192/1024/1024/512 上限、16 请求上限、聚合、答案提取和显式 `thinking_mode=False`。不通过 client 属性、类名或签名推断能力。
- 原 `combined` 固定五项原始开关；新增 B1 不自动进入旧组合。
- 证据：旧实测第 30 题三个生成请求均为 stop，却返回工具调用文字，普通候选同样受领域系统提示影响。B1 解决提示矛盾，不保证模型从此不输出工具文字或全部答对。

## B2：专用简短补答提示

独立开关/计划名：`concise_recovery`，默认 False。只把 `_quick_fallback` 的系统提示替换为 `CONCISE_RECOVERY_PROMPT`，要求单行最终答案；原用户提示已经要求直接回答，因此逐字保留。512 上限、温度 0、思考 False、16 请求预算和候选资格不变。三种现有恢复来源共用；不增加恢复轮次，不从 length 内容中抢救临时答案。旧系统要求思路和推导，用户却要求直接给答案；先消除矛盾，再通过独立实验决定是否加预算。B1 已单独提交为 `6c775a4`，B2 以 `feat: add isolated concise recovery experiment` 独立提交，实际提交号记入本地交付回执。

## 离线验收

B2 首次完整测试为 717 通过、1 失败：历史测试冻结所有 `_PROMPT` 全局变量，新增默认关闭的 B2 提示也触发比较失败。按本轮已授权的独立实验范围，冻结测试改为逐项比较全部旧提示及完整 AgentConfig AST，只登记 `CONCISE_RECOVERY_PROMPT` 这一新增项；任何其它新增提示仍失败。另检查默认补答实际发送旧提示、显式开启才发送新提示，用户消息、四参数和 512 上限一致。失败日志保留为 `b2-formal-py312.log` 与 `b2-pytest-diagnostic.log`，后续验收另存文件，不覆盖失败证据。

B1 验收完成：定向 85 项通过；Python 3.12.14 完整 formal 688 项通过（44.10 秒），Python 3.10.11 完整 formal 688 项通过（50.85 秒），保留 60 秒限制。Ruff、61 个 Python 文件的 3.10 编译、git diff 检查和 few-shot 离线预览通过，零真实 API。

冻结 18 领域及通用提示摘要；验证领域数学行逐字保留、所有候选来源、显式工具适配路径、请求参数、配置、输入和引用隔离；严格四参数 client 与外部同名模块污染；单变量计划和原组合不变。使用 `git show 9acff7e` 还原正式五文件闭包，19 个路由 × 7 类固定响应情形共 133 个默认行为对照全部相同（精确比较请求和 final_response）；包括补答、补答仍截断、工具文本、反思、传输异常和预算耗尽。该结果只证明这些离线情形，不替代模型正确率。

证据输出位于被忽略的 `outputs/b1-b2-offline-20260908/`：`b1-formal-py312.log`、`b1-formal-py310.log`、`b1-default-request-audit.json`、`b1-fewshot-dry.log`。领域摘要回归固定在 `tests/fixtures/b1_default_domain_hashes.json`。

B2 与最终组合代码验收完成：最终定向 178 项通过；Python 3.12.14 完整 formal 718 项通过（43.22 秒），Python 3.10.11 完整 formal 718 项通过（53.86 秒）。两个版本均未放宽 60 秒限制；全库 Ruff、61 个 Python 文件的 3.10 编译、diff 检查及 21 个 few-shot 离线预览通过。最终代码重新执行 133 个默认行为对照，全部一致。

新增证据：`b2-final-formal-py312.log`、`b2-final-formal-py310.log`、`b2-default-request-audit.json`、`b2-fewshot-dry.log`、`b2-changed-final.log`。运行源码摘要（Q1 source_hash）为 `95e9cd8c5707479e0e0c2189ce56ec8d58183df952e51c76267d43b9bfce1610`。`main` 保留 `9acff7e`；B1/B2 开关默认 False，无真实 API、推送、正式提交或准确率晋级。

`dev-plan.json` 已按新源码生成，包含 baseline 与两个独立候选；`q2-study` 已冻结、读取复核通过，仅登记 B1/B2 两个候选。Q2 协议 SHA 为 `03ae57004e74d18102c41e871c08a66bfdbb58af84a68b28046d484fed8b46fd`，状态仍为 development，未预约运行、未执行。Q2 文件中的完整重复矩阵上限是未来证据框架，不是下一轮调用清单或额度授权；下一步仍按下列小批筛选方案单独决定。

## 后续小批实测方案（尚未执行）

1. B1 单阶段筛选：从许可明确的冻结开发集预先选定 4 个输入，覆盖有工具指导的不同领域及一个正常完成对照。同题基线/新策略各一次，合计最多 8 次 HTTP 发送、输出上限 65,536 token。固定模型为 397B、思考 False、温度 0.6、单次上限 8192，仅改变生成提示。
2. B2 单阶段筛选：预先冻结 8 个恢复输入，基线/新策略各一次，最多 16 次 HTTP 发送、输出上限 8,192 token，温度 0、思考 False、单次上限 512，仅改变补答提示。
3. 每批派发窗口最多 10 分钟，沿用既有控制器停止机制；在途请求另行有界收尾并记录成本。因此不承诺十分钟内完成全部请求。输入 token/货币成本须执行前另列，不启动额外重试或自动续跑。
4. 正式开始前冻结具体输入 ID、代码/配置/数据摘要及限额，按当次用户授权执行。只使用开发集；标签仅用于事后核验，不传给模型。单阶段筛选看工具文字停滞、完成性、答案正确性与成本，不能代替整题或 Q2 留出验收。
5. 新提示改变请求，必须取得新响应。旧记录未发送 thinking_mode=False，不能作为当前请求的严格回放或新基线；旧补答/校验截断比例只保留作历史诊断。
