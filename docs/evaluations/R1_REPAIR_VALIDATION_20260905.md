# R1 修复与复验记录

## 范围与验收计划

基线：`2cf691fb6f3b61380704a15b6e5a80d4664bef31`。用户明确要求修复独立验收缺陷并完成 R1；历史失败证据保留在 `R1_OFFLINE_ACCEPTANCE_20260905.md`。

修复限定于 R1 的公开调用、构造、trace、生命周期、截断与导入边界，以及显式本地适配器的请求归属。保持 prompt 文本、候选数、温度、token/request/tool/deadline 配置和合格答案的多数票排序不变。不恢复归档 S1–S6，不新增依赖，不进行数学能力调参。

计划顺序：固定失败反例 → 构造/生命周期/trace → 候选资格与恢复 → 请求绑定 metadata → 导入所有权 → 全量离线验收。每个表面独立复验，正式提交前仍须以可追溯候选和单变量计划审查，不能用本次累积离线修复冒充多轮单变量官方成绩。

旧并发探针的 `chat(); read_usage()` 改用请求返回的 `complete()[metadata][usage]`；保留同一双线程交错和 `(11,29)` 断言，因为修复将删除有竞争的最近响应接口。其它失败条件不放宽。

验收性质：未知 client 只有三参数调用；任何正常异常不逃出 solve；预检失败零请求；公开 trace 不含任何模型/题面/异常原文；无合格答案时失败关闭；隔离候选不能经反思、恢复或最终选择复活；每次恢复共享单题预算；单题 usage 与并发请求一一对应；同名污染不影响正式入口且不改写外部模块缓存；根文件闭包和仓库外隔离加载成功。

## 修复结论（2026-09-06）

F1–F6 的代码修复和完整离线复验已完成。当前交付为基于上述基线的未提交工作树，不是已发布版本；没有执行提交、推送、真实模型请求或第二次官方评测。机器阶段保持 R1，不声明正式退出或正确率提升。

| 缺陷 | 修复结果 | 固定反例及补充验证 |
| --- | --- | --- |
| F1 构造器不兼容 | 接受额外位置和关键字参数；仅本入口的真实 AgentConfig 成为配置，不探测不透明对象或未知 client | 三种构造陷阱、严格三参数 client、私有属性陷阱 |
| F2 trace 泄露 | 集中出口只保留白名单阶段及有界数值预算，原始内容统一省略；最多 256 事件 | 模型/题面/异常/嵌套内容、选择事件，以及 6 类模拟敏感内容 × 4 个位置；幂等性质 |
| F3 生命周期异常 | 配置、输入序列化、预算初始化、求解及最终输出均在公开失败边界内；异常稳定返回未解出 | 深层 metadata、初始化故障、各阶段故障；预检零请求、预算上下文清理、协议异常不盲目重试 |
| F4 截断候选复活 | 初始、反思、直接恢复统一校验答案资格；无合格候选即未解出，不从残文重新抽取 | 四阶段 length、空/残句/未闭合 boxed/异常恢复、预算 1–4；原有多数票排序不变 |
| F5 导入所有权 | 从入口文件目录将四个既有源文件装入新建项目私有命名空间，不覆盖外部同名模块，不修改 sys.path | 单项及九个名称联合污染、根文件复制闭包、仓库外 python -I 加载；正式闭包不需 SymPy |
| F6 并发 metadata 串题 | response 与 metadata 随请求返回，删除最近响应接口；本地工具末轮也记账 | 强制线程交错 (11,29)、同一 Agent 并发单题预算、本地真实数学工具端到端 3 请求/1 工具/45 tokens |

正式门禁已加强：`python .agents/policy_guard.py --formal` 在静态扫描通过后实际运行整个 `tests` 目录；必需回归缺失、suite 失败或超时均阻断。增加成功和失败的门禁自身测试。单独调用静态 `evaluate(..., formal=True)` 仍不能替代这个完整命令。

## 验证证据

| 检查 | 结果 |
| --- | --- |
| 历史独立验收基线 | 19 项中 17 失败、2 通过；原否决报告不改写 |
| Python 3.12.13 完整 formal | **187 passed in 9.96s，PASS** |
| Python 3.10.11 完整 formal | **187 passed in 11.42s，PASS** |
| 事前规则匹配及修改后门禁 | PASS，阶段 R1 / policy_version 2026-09-05.1 |
| Ruff、变更空白检查 | PASS |
| 两版本运行时、本地适配器、评测、测试和门禁编译 | PASS |
| verify_math 离线预览 | 21 个示例；零真实 API 请求 |
| prompt 与默认配置冻结 | AST 对照 2cf691f 通过；默认候选数、温度及预算不变 |

187 项由原 103 项、19 项固定验收回归、63 项状态/内容/隔离/并发性质用例和 2 项门禁回归构成。并发旧探针仅适配已删除的最近响应协议，不改变线程交错和请求归属断言。测试均使用确定性模拟 client；工具端到端测试执行真实受限数学计算，不访问模型服务。

原始本地证据位于 `outputs/r1-repair/`：`formal-py310.log`、`formal-py312.log`、`changed.log` 和 `source_manifest.json`。该目录被忽略，不属于正式发布材料。manifest 记录逐文件 SHA-256 和聚合指纹，覆盖活动运行时、本地适配器、门禁与全部测试；其候选标记明确为未提交工作树。后续任何源文件改动都须重新取得相应证据，不能复用此结果冒充新提交的检查。

本次 34 个源文件/测试文件的聚合指纹：`e99412b0cf2c72d80d544326a497922ea16a12b2367bc66d414d718f58483bcd`。算法为逐文件原始字节 SHA-256，再对按路径排序的紧凑 JSON 路径/hash 映射求 SHA-256；不含会在复验后追加结果的文档。

## 适用边界与 R1 剩余退出步骤

本次只验证 Windows 上两个 Python 版本。当前没有可用 WSL/Docker，未验证 Linux 执行；隔离导入测试覆盖加载方式，不等于正式 runner 的操作系统验证。正式 client 未提供 finish_reason 时，仅可保守识别缺失答案标记、残句和部分未闭合表达式，不能证明任意自然语言或数学表达式语义完整。公开 trace 不含原文；final_response 按原契约仍包含获胜候选推理。

完整 R1 的最后一项仍是第二次官方运行保持请求链有效。下一次模型按用户确认为 `intern-s2-preview-35b`，提交页模型选择回执仍须保存；没有把该名称擅自写入本地通用 API allowlist。若仍为 112 题，默认每题最多 16 次 Agent 层请求，合计上限 1792 次；平台内部重试和计费额度未获回执，不据此给出货币估价。

进入外部执行前需：将修复整理为可追溯候选提交并再次通过同提交门禁；核对实际 AtomGit 组织/仓库、远端 main 与模型选择；按项目规则确认本次提交和额度授权。官方实验应记录本次是相对 2cf691f 的累计契约修复，不能将结果归因为任一单项；如要求单变量归因，必须拆分候选和运行。提交到抓取完成期间冻结 main，并保存抓取 SHA、批次、请求数、逐题 success/error 与判分日志，再判断 R1 能否退出。`OFFICIAL-GAP-*` 保持原状态。
