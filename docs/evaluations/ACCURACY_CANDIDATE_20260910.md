# 2026-09-10 联合提分候选与完整诊断

状态：三项工程与完整本地诊断已完成，实际部署开关已开启；尚无本候选正式分数。目标为下一批超过 35%（112 题至少 40 道），本轮证据不足以保证达到目标。

用户明确剩余四次正式测试，授权联合修改三项：英文/中文数学路由、公开题完整候选到选答损失诊断、实际失败积分题族的有界精确核验。正式隐藏题及逐题过程不可见，所有诊断来自已登记许可的公开开发题；已用于开发的题不称留出或盲测，不把官方汇总推造为逐题信息。

基线为已正式运行的 b87e4b9f2f7383960c659b887c39daa809f228cb，其运行源码与本地 5574333 相同。使用不可变源码快照保留旧配置；候选另存快照和源文件摘要。旧实验 Q1Policy 的全关闭配置须保留，正常部署入口的实际启用策略单独登记与测试。

本地线上完整诊断最多两版各 3 道公开题，总计 96 次请求、累计请求输出额度 300000 token、全局派发窗口 90 分钟；每版 48 次/150000 token，单次原 345/350 秒等待限制不放宽。实际费用由账号计费，未取得货币单价不编造费用；会记录实际 usage 与未知用量。没有自动重试、旧运行续跑或正式评测提交。候选标签仅在外部审计读取，不传入 solve 或 metadata。

本轮保留根 ReasoningAgent、六源文件私有导入、四参数 thinking_mode=False、原候选数/温度/模型/输出上限和每题预算。新增策略不引入正式网络、私有 client 访问、外部服务、root 模块或依赖。所有已知碰撞、严格 client、输入边界、截断、预算、答案交付、trace 和有界数学安全测试须通过。

## 1. 实际启用与保持项

普通 `ReasoningAgent(client)` 调用 `deployment_policy()`，四项为 True：`concise_recovery`、`bounded_math`、`evidence_selection`、`reasoned_verifier`。双语路由直接生效，没有额外总开关。B1、检索、条件提示及其余旧 Q1 选项保持 False。显式 `Q1Policy()` 是全关闭策略配置，但仍含新路由；严格旧基线必须加载上述 Git 快照。

运行时仅改 `user_agent.py` 与 `domain_prompts.py`。私有六源导入、注入 client 的四参数 False、AgentConfig AST、候选数/温度/8192/1024/512 上限、每题预算、依赖及语料全部保留。

## 2. 完成的三项工作

1. **路由**：英文完整词、中文和数学记号匹配，最长术语优先，同分/不确定时回通用提示。修复 Express→Res、determine→det/min、triangle→tr 等误判。72 道已公开开发输入人工主题审计：34 项明确纠正、19 项移除错误专门化、15 项不变、4 项混合主题待定；不是新增答对题数。旧领域提示常量全部保持。静态词表预编译前后 1672 个输入的完整命中词与领域一致；缓存不保存题面/答案。
2. **完整求解诊断和选答**：授权的本地观察器记录全部候选、验证、聚合与终答，不改变公开 trace。修复整组否定票规则：仅在存在另一个获肯定的答案组时排除整组全否定组，组内有 pass/unknown 保留。验证器先给短核算再裁决；完整数学证明优先于模型投票。所有候选被精确证明错误时，在原预算内追加至多一次定向重算；新答案 fail/截断拒绝，unknown 可交付但不称已证明正确。
3. **实际积分核验**：支持有界有理函数、一个二次/常数根式、常数倍 ln/atan 原函数，先证明全实轴定义域，再做精确求导恒等式；完整解析实际多行题面与 where 整数参数块，要求 +C。缺条件、不同根式、三角/指数或资源超限返回 unknown。原 36 份保存响应得到 2 pass/2 fail/32 unknown，正确区分两条积分失败案例；72 题输入覆盖为 2/72，覆盖仍很窄。没有把有限点吻合当证明或引入正式 SymPy。

## 3. 真实 API 与数学复核

输入 SHA256：`49a2465c71eaa947dc22328d7c50ff1a5f601be9f92b9f4d9801889f50e8e856`。题集是 MIT 许可公开开发题的已暴露位置 0/27/29，不是盲测/官方题。求解仅收到题面与空 metadata，参考与许可证信息另存审计文件。数学判定由独立审查者手动转录为安全程序化 SymPy 表达式、精确求导和域证明完成；未直接执行模型字符串，未只依赖新核验器自身作 oracle。

| 项目 | b87e4b9 旧源码 | 原始线上候选 |
| --- | ---: | ---: |
| 完整终答数学正确 | 3/3 | 3/3 |
| 9 个保留候选的数学正确数 | 6 | 7 |
| 至少一个正确候选 | 3/3 | 3/3 |
| 已有正确候选却选错 | 0/3 | 0/3 |
| 模型请求/实际尝试 | 19/19 | 20/20 |
| 请求输出额度 | 83456 | 84480 |
| prompt/completion token | 11530/7530 | 16676/13168 |
| 实际总 token | 19060 | 29844 |
| 三题求解累计秒数 | 352.203 | 498.707 |
| 显式截断请求 | 0 | 1（验证阶段，按 unknown 处理） |
| 未知用量/重试 | 0/0 | 0/0 |

共 39 请求、48904 实际 token，未超过授权的 96 请求/300000 requested output/90 分钟。模型回执为 `Intern-S2-Preview-397B`，实际发送四参数且 False；无隐藏 reasoning_content。没有触发正式评测。新版耗时和 token 均更高，不能声称效率改善。

基线的三个错候选中有两个获模型 A；新候选原始解析为 2 pass/2 fail/5 unknown，其中存在一次错误否定正确候选。读取原始完整标签则为 5 A/3 B/1 截断：两次 B 错误否定正确答案。两版生成候选不同，不能把两组计数作同候选受控因果比较。精确证据保护了正确积分候选，但本轮完整终答增益为 **0 题**，不得把 3/3 外推为正式 35% 或任何总体正确率。

另一个已观察限制：一份生成响应数学正确却未给规范最终答案、末尾还要求工具调用，触发已有补答后反而生成错误候选；其它候选保住终答。本轮没有放宽截断/答案资格来吞入不完整文本，该问题保留为后续生成与交付诊断。

## 4. 最终空行修复与严格回放

原始线上候选 `user_agent.py` SHA256 为 `ce8c88bf3be69fcd59b96fe5063c60b082391e100462787fb8bf6c2a3004fa15`。实测完整 CHECK 和 VERDICT 之间存在纯空行，原严格两行解析把它们记为 unknown。最终仅在 `_reasoned_verdict_status` 忽略纯空白行，仍拒空 CHECK、断行标签、重复标签和 length。

最终入口 SHA256：`3bb89fd5bd7cbe7c1b4f2bcde42b1ad4d662f4c6b74109853cc7caae7fa2a321`；领域模块 SHA256：`78fa377794dea498bb52444b1e1c99d69942fcffcd4d0ace72ceb9b8792f1f15`；六源加资源总摘要：`7f47770e69bf07392da875db0ebf226a8fa388c616dfddea7429f76a619d035e`。

冻结候选自回放和最终默认入口回放均完整消费 6/6/8 共 20 次真实录制；每次四参数精确相等，原始证据哈希不变，0 个新 API 请求。三个最终答案行全部相同；第一题获胜推理正文因置信度恢复而改变，其余完整 final_response 相同。原始 1 次截断仍为 unknown，没有把截断答复恢复为肯定。最终 replay 得 5 pass/3 fail/1 unknown，仍有两次模型错误否定，由数学证据保护。此为记录响应上的集成证据，不是重跑最终源码所得的新线上成绩。

## 5. 验收与审查

- 最终 Python 3.12：完整 formal **897 passed，46.80 秒**；Python 3.10：**897 passed，53.10 秒**。原 60 秒门禁未放宽。Ruff、71 个 Python 文件编译、政策检查与差异空白检查通过。
- 初次并行工作期间完整门禁超时，随后发现一个仍将旧实验配置与新默认配置混比的 corpus 回归测试。现分别验证两种配置，保留旧断言并增加部署用例；失败记录与最终报告分开保存。
- 独立计算审查 555 例：330 pass/210 fail/15 unknown，未发现错误数学判定；17 个含重复实根用例正确拒绝严格正性。另有 80 个程序化 SymPy oracle 断言、表达式资源对抗和原始积分反例。
- 独立审查推动修复逐候选删票误伤多数组、反思 fixture 检查错误消息位置、裁决正则吞换行及纯空行兼容问题。所有最小反例保留。
- 所有公共 client、官方预加载、同名污染、私有属性陷阱、根入口、最小闭包、截断资格、预算和公开 trace 安全测试保留；本地通过不关闭 `OFFICIAL-GAP-*`。

本地证据在 `outputs/accuracy-20260910/`：`live/`、`baseline-math-review.json`、`candidate-math-review.json`、`final-strict-replay.json`、`validation-py312-delivery.json`、`validation-py310-delivery.json`、路由/数学独立审计。原始模型交互不进入提交源码。

## 6. 交付边界

这是已启用的联合候选，仍是未获正式成绩的提分实验。数学核验覆盖有限，模型 CHECK 可能计算错误或超时截断，单轮多数票与 unknown 新补答仍存在选错风险。不能保证下一轮至少 40/112。

2026-09-10 只读核对时，GitHub 与用户提供的 GitCode 比赛仓库 main 都为 `b87e4b9`。工作分支已安全快进到同树的该提交后准备本轮新提交；此次尚未推送主线或发起正式提交，不能将本地完成称为平台已经使用新版。
