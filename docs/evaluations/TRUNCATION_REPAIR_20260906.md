# 2026-09-06 截断缺陷修复与验收

## 范围与决策

基线为 `6d42d2c`，用户在专项审计后要求直接修复，并确认单次 8192 为官方数据。本轮代码落实 8192 硬约束，不等待额外材料；证据身份仍记用户转述，不伪造官方原件。本报告描述未提交工作区，不代表正式平台已经评测此修复。

本次是明确授权的累计缺陷修复。分别用旧版失败反例验证每项语义，保持模型、prompt、默认候选数、温度和默认输出预算数值不变；不将完整修改误写成单变量模型收益。运行时仍只调用未知 client 的三参数公开 chat，不增加模块依赖、不读取私有字段、不通过同名类判断能力。

## 已修复的代码问题

| 问题 | 实现后的行为 | 回归证据 |
| --- | --- | --- |
| TRUNC-02：生成预算耗尽丢答案 | 生成循环本地捕获 BudgetExceeded，保留已完成候选；统一资格检查后聚合；未合格残文不复活 | 1/2 请求上限、工具/纯推理路径、整数/分式/根式答案，虚拟 601 秒期限；无合格候选仍未解出 |
| TRUNC-03：未知验证被当错票 | 默认只接受完整 VERDICT: A/B 或单独 A/B/CORRECT/INCORRECT；其它、矛盾或截断文本为 unknown；unknown-only 不触发 critic/reflection | 空白、残缺、含否定句、混合标签；默认及 Q1 严格模式；明确 B 仍可触发批评 |
| TRUNC-04：stop 覆盖截断 | 响应本体或请求 metadata 任一提供未完成状态，即保留该证据，stop 不能取消 | length/content_filter/tool_calls 与 stop/空/缺失/length 双方向组合 |
| 明确 length 的空内容错误退出 | 仅 content=null 且有明确 length 时视为不完整空响应，进入原预算内恢复 | 成功恢复正例；无 finish reason 的 null 仍按协议异常失败关闭，避免泛化容错 |
| 8192 硬上限缺失 | 配置预检和 `_chat` 请求边界均检查整数 1–8192；全部四种阶段输出预算受限 | 8193/16384/极值配置零请求；布尔/0/负数直接调用拒绝 |
| 诊断计划超过上限 | 取消 16384 条件，改为 4096 与 8192 上限内比较，payload 再次检查 | 诊断程序自身拒绝超过 8192、布尔类型；旧计划因源码/条件指纹变化不能执行 |

Q1 `calibrated_verifier` 不再独占 unknown 保护，而仅进一步要求完整 VERDICT 标签、拒绝单独 A/B/CORRECT/INCORRECT。该变量仍有独立区别，但所有旧计划均须按新代码重新生成，不能沿用旧比较结果。

## 先失败再修复

新增 `tests/test_truncation_repairs.py` 的 65 项反例先在旧实现执行：**45 failed / 20 passed**，保存在 `outputs/truncation-repair-20260906/before.log`。未将失败标成 xfail，也未删除旧失败证据。修复后 65 项与诊断工具 16 项合计 81 项通过。

第一次全量检查另发现两个旧断言固定了错误行为：`test_budget.py` 和 `test_lifecycle_fallback.py` 要求首份完整答案在后续生成预算耗尽时丢弃。按本次明确修复目标改为同时断言完整推理/答案保留、仅一次请求、generation_budget_exhausted 事件；新增无合格答案反例仍要求未解出。它们没有放宽请求预算或允许残句。修订前全量结果为 2 failed / 450 passed，保留在 `full-first.log`。

修订后的针对性检查共 88 项通过。最终完整门禁：

| 检查 | 结果 |
| --- | --- |
| Python 3.12.14 `--formal` | 452 passed / 11.60s，PASS |
| Python 3.10.11 `--formal` | 452 passed / 12.74s，PASS |
| 全仓 Ruff / 改动文件编译 / diff 空白检查 | PASS |
| 新计划与当前源码、冻结题集一致性 | PASS；18 请求、每次不超过 8192、63488 输出 token 上限 |
| 真实 API / 正式平台 | 本轮未执行，不能宣称截断率下降 |

452 项由原 371 项、诊断工具 16 项、本轮修复 65 项构成，全部实际执行，无 xfail。完整日志和 14 个相关源文件指纹在 `outputs/truncation-repair-20260906/`，`source-manifest.json` 的 source_sha256 为 `c9b2c7fb5e1b2c4df92be3308eaec20d9ae03cb2de5cbff371d45876d4236657`。此次关闭已复现代码缺陷的离线验收；真实根因验证和正式验收仍未关闭。

## 已准备且尚未执行的真实诊断

新计划：`outputs/truncation-repair-20260906/diagnostic-plan.json`。
计划 SHA-256：`30e10bf95765f9b6bd3f0408906d56aef00d01f11992e02d9070adeca622edce`。

仍使用已冻结公开开发集的两题，每阶段三条件：兜底 512 默认 / 512 关闭思考 / 4096 默认；验证 1024 默认 / 1024 关闭思考 / 4096 默认；生成 8192 默认 / 8192 关闭思考 / 4096 默认。模型固定 397B，普通 API 的关闭思考条件只用于本地因果诊断，不传入正式未知 client。

上限 **18 次 HTTP 尝试、零重试、63488 输出 token（另计输入）**。遇 HTTP 错误、协议错误、模型回执缺失或不符立即停止；不自动追加额度。价格未取得回执，不能承诺货币成本。每次请求均不超过 8192，不消耗正式每日测评机会。旧 `a7289278...` 计划和旧 88064 token 口径已失效，保留为历史方案。

## 仍不能凭代码保证的部分，以及需要用户做什么

1. **真实输出完成率与最优预算**：模型的内部思考、8192 之内的实际生成需求、普通 API 文档记载的 120 秒服务端中断，不能用 fake client 定案。下一步需要用户授权上述有明确上限的真实调用；已有 key 可用于该诊断，不需换 key，也不需先提交作品。
2. **正式扩展接口**：若希望正式关闭思考，需要主办方确认“注入 client.chat 是否保证接受 thinking_mode=False，平台是否覆盖该字段”；若没有保证，继续采用三参数，不靠本地支持推断正式支持。此答复有助于选策略，但不是本次代码修复或上限内诊断的前置条件。
3. **正式实际模型和元数据**：可得的模型回执及 finish_reason/usage 公开契约有助于对齐。只返回纯文本时不能完整识别所有截断，不能通过私有 getter 恢复，也不能要求用户取得隐藏题目或判分细节。
4. **最终正式效果**：修复可避免已知程序丢弃答案和无依据额外纠错，不保证隐藏集正确率提升或零 invalid。真实小诊断确认机制后，再按开发集/留出集及实际正式抓取提交验证。

阶段仍为 R1。未提交、未推送、未调用真实 API；默认生成 8192 / 验证批评 1024 / 兜底 512 的数值暂不凭猜测调整。普通 API 与平台时间限制不能通过放宽本地超时绕过。
