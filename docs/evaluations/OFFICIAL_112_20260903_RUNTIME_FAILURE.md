# 2026-09-03 第四次官方 112 题请求前故障与根模块回迁

## 1. 证据范围

- 原始脱敏日志：`eval_log_77f60c29c17c44098e0e9cf7f2336ef8.log`
- 日志 SHA-256：`acbf23b0486f306e92c9237977041b59d69b3562cd4a9995cfb3deeb955445a8`
- 平台实际拉取提交：`05ec5c276f6d9a6d2c536fdff9efd45f4d6e8456`
- 输入 SHA-256：`7f2499c53f52cbcb17dcab7cc4b99c9e79f53e23c1392587289a02695284201f`
- 平台可见环境：Python 3.11、lagent 0.5.0rc3
- Agent 阶段：2026-09-03 11:07:53.670Z 至 11:08:08.613Z，约 14.9 秒

本报告只保存聚合指标、版本和可复现的条件性实验，不保存题面或完整响应。平台日志没有逐题异常、worker 源码、实际构造参数、`sys.path` 或 client 类型，因此不能从该日志直接证明唯一根因。

## 2. 聚合结果

| 指标 | 值 |
| --- | ---: |
| 题目总数 | 112 |
| success / error / skipped | 0 / 112 / 0 |
| correct / incorrect / invalid | 0 / 0 / 112 |
| client request / attempt / retry | 0 / 0 / 0 |
| prompt / completion / total tokens | 0 / 0 / 0 |
| runner 状态 / 退出码 | completed / 0 |

依赖安装成功，runner 也完成聚合；但 112 题没有产生一次模型请求，并在约 14.9 秒内全部进入 error。因此这仍是请求前运行时故障，不是数学正确率为 0%，也不能用于判断截断、能力或保守内核效果。

## 3. 结构相关性

| 提交 | 正式部署形态 | 官方结果 |
| --- | --- | --- |
| `350a267f` | 根目录扁平模块，`user_agent.py` 含主要编排 | 1082 请求，23/112 |
| `70ac53f` | `user_agent.py` 导入 `math_agent` 包 | 0 请求，112 error |
| `c088a84f` | `user_agent.py` 导入 `math_agent` 包 | 0 请求，112 error |
| `eb5d8d47` | `user_agent.py` 导入 `math_agent` 包 | 0 请求，112 error |
| `05ec5c27` | `user_agent.py` 导入 `math_agent` 包 | 0 请求，112 error |

包化实际始于 S1 提交 `3ba0610`；`70ac53f` 是其后的 S2～S4 分层锚点，不是首次引入包目录的提交。五个被评测版本呈现完全结构相关性，且前三轮针对私有方法、三参数投影、构造器和保守协议的修复都没有恢复请求。由此，物理包目录成为当前最值得隔离的部署变量。

相关性不能升级为“judge 只复制根 `*.py`”的事实。最新日志明确记录平台先把完整用户仓库克隆到 `/opt/atomgit/math-agent-xh202627-v3`，并把入口指向该目录下的 `user_agent.py`；日志没有说明独立题目 worker 是否再次选择性复制文件。官方材料也没有公布这一实现。因此准确结论是：**根模块回迁具有历史成功锚点，是当前合理的单变量兼容实验；包目录在线不可见仍是待验证假设。**

## 4. 条件性复现与新门禁

在只保留仓库根目录 Python 文件的临时环境中：

- `05ec5c2` 的 `user_agent.py` 无法找到 `math_agent`，在首次请求前抛出 `ModuleNotFoundError`；
- 根模块回迁后的入口可由 `python -I` 通过绝对路径加载；
- 严格 fake client 的签名仅为 `chat(messages, temperature, max_tokens)`，实际完成 3 次候选和 3 次 verifier，共 6 次调用；每次键集合严格等于这三个公开参数，并输出唯一末行 `最终答案：4`；
- 临时环境没有 `math_agent/`，所以该测试不会被本地完整仓库掩盖。

这证明回迁版本兼容“只提供根文件”这一条件，不证明正式平台一定采用该条件。

## 5. 实施边界

远程提交 `d9f7585b4dc19d55d16c1b72faf3cd9d8fb5d3ae` 已把 29 个运行模块移回根目录并改写运行时导入。拉取后本地复核发现该提交的机械替换同时造成 11 个测试收集错误、发布白名单空前缀、缺失的根 `__init__.py` 必需项，以及把完整异常栈回显到 `final_response` 的泄露风险。修正工作树保留扁平部署，但恢复 `scripts/` 和 `evaluation/` 自身的包内相对导入，修正客户端/工具模块导入、显式列出允许发布的根模块，并删除异常正文回显。

以下内容保持不变：模型 allowlist、三个纯文本候选、每候选一次 verifier、正常 6 次公开调用、温度、token 上限、截断恢复、预算、选择规则和最终输出协议。没有调用真实 API。

受影响规则和问题为 `ARC-001/005/006`、`ARCH-001`、`ENTRY-001/003`、`CLIENT-001`、`COMPAT-CORE-001`；`OFFICIAL-GAP-CLIENT/ERROR/RUNNER/CHANGE` 继续未关闭。根模块回迁不得解释为已证明线上恢复，也不得撤销 S2～S4 的显式上下文、统一网关和职责拆分。

## 6. 下一次正式评测

最终工作树的完整离线质量门禁为 21/21 项通过：203 项测试通过，总语句覆盖率 77.27%；Ruff、全部根运行模块与评测/脚本编译、Bandit、Python 3.10/Linux 锁闭包、`pip check`、三套依赖漏洞审计、敏感信息扫描、Markdown 链接、竞赛合规、few-shot dry-run、runner 和全部评测 CLI 均通过。首次含漏洞审计的运行只因沙箱禁止访问 PyPI 而失败，联网查询公开漏洞数据后同一代码检查通过；全程没有调用模型 API。

下一次只验证部署兼容性，依次检查：

1. 平台实际拉取 SHA 与提交 SHA 一致；
2. `request_count > 0`、`attempt_count > 0`、`success > 0`；
3. `error == 0`、`invalid == 0`；
4. 前三项成立后，才分析截断率和正确率。

若扁平版本仍为 0 request，不再继续根据结构相关性猜测，应立即索取逐题脱敏异常类型、worker 文件清单、入口加载代码、构造参数、`sys.path` 和 client 精确版本。只有绑定新 SHA 的有效官方运行才能把 `ENTRY-003` 从“线上待验证”改为已解决。
