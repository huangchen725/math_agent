# 2026-09-07 公开开发集正确率首轮实测

当前状态：**v3 已按用户要求暂停：baseline 完成 72/72，compact_routing 完成 11/72；两个执行进程均已退出，不自动续跑**。此前 v2 因读取超时停止，基线完成 6/72 题，compact_routing 完成 0/72；原结果完整保留，不计入新批次分数。用户已明确回复“授权”，批准 baseline / compact_routing 首轮配对，共 144 次 solve、最多 2304 次真实请求，输出保守上界 18,874,368 token；输入用量和实际货币费用未知。用户随后要求留在当前任务等待完整结果；v2 触发原停止规则后，用户又明确同意重开并授权等待不超过 360 秒，最终因耗时过长明确要求暂停。公开留出集及后续重复实验不在本次执行范围。

## 冻结身份

- 基础提交：`35fbea6f346095444a5355dc6d96106afb1ad45b`；真实运行期间只改执行辅助文件和报告。中止后另修复离线 judge 并保存新旧口径；正式求解逻辑未改。
- 运行源码摘要：`f34165c141e3aaede67fab4b7c755d4d1da7e74a11a10dcbd71c65b9eb74d791`。
- 分析器摘要：`dd8387fd46e07348758a13c9a9380c16bd38a110c907b5f0378ac61c9db31e98`。
- U-MATH 冻结数据摘要：`b821cdddd56ef3276788e7307bdd7838f0fbe7f3817d0835eab4c092f5b5db27`，只运行 72 道 dev 题。
- 目标模型：`intern-s2-preview-397b`。实际首个响应 model 为 `Intern-S2-Preview-397B`；记录原值，只允许 ASCII 大小写归一，不接受 35B、缺失标识或其他别名。
- 唯一策略变量：`Q1Policy.compact_routing`。其余配置、候选数、温度、验证/反思和 token 上限一致。
- 顺序：先 baseline 后 compact_routing，各完成整个开发集；时间顺序可能混入服务状态变化，单轮不能当作稳定收益。

## 启动问题与额度账本

联网执行身份不能读取原题集的受限文件，首次预检失败发生在任何 API 请求之前。保持题集 ACL 不变，离线控制器读取题目/参考并评分，联网中转只接收求解器产生的模型请求，不读取题集或参考文件。

自动审批曾因担心客户端重试突破上限拒绝启动；改为 `HTTPAdapter(max_retries=0)`、`allow_redirects=False` 的单次发送后通过。成功、传输失败、重定向反例均验证一次发送；控制器和联网进程分别限制总请求和请求输出预算。遇到传输或协议失败立即停止，不自动恢复、重试或跳过不利样本。

第一次实际请求返回成功，但本地按大小写严格比较 model 字段导致中止。该请求消耗 prompt 486、completion 780、total 1266 token，finish_reason=stop。没有完成首题，也未查看或使用答案进行评分/选策略。原始研究、凭证、输出、执行脚本和失败记录完整保留在 `outputs/accuracy-20260907/`，没有清除 attempt 标记或复用原研究。

在相同求解参数下新建替代研究，唯一执行层修正是允许模型名仅大小写不同并同时保留原始标识；已测试 `35B`、含糊别名及缺失 model 继续被拒绝。之前已用 1 次计入用户总额度，替代研究剩余最多 2303 次请求、18,866,176 请求输出 token，合计仍不超过原授权。

| 文件 | 作用 |
| --- | --- |
| `outputs/accuracy-20260907/pilot-plan.json` | 原授权方案，SHA `babdaf41746c19b37cc70fc6f7607edd02bd20ca9abc871275b066be54f63616` |
| `outputs/accuracy-20260907/incident-model-case.json` | 原研究中止原因与已消耗额度 |
| `outputs/accuracy-20260907-v2/pilot-plan.json` | 替代执行方案，SHA `6ea75f58e87c3e5c8bb5aa4bb9e2279b0e9f53cff82cdb5b661e66d1af8fcf2f` |
| `outputs/accuracy-20260907-v2/execution-started.json` | 启动身份、执行器摘要、时间与授权关联 |
| `outputs/accuracy-20260907-v2/execution-status.json` | 替代批次尝试、完整回执数、token 与停止状态 |
| `outputs/accuracy-20260907-v2/request-ledger.jsonl` | 逐请求开始/完成时间、阶段、原始/规范化 model、usage、finish_reason |
| `outputs/accuracy-20260907-v2/runs/` | 两组 checkpoint 与运行清单；未完成时不得当作完整分数 |

辅助执行器位于被忽略的 outputs 内，不进入正式运行时。实际发送体只有 model、messages、temperature、max_tokens，未启用工具扩展、stream、n 或修改 thinking_mode。逐请求元数据只在本地显式适配路径采集。台账不记录 Authorization、密钥、请求 ID 或异常原文。

## 中止原因与实际用量

替代批次第 37 次请求在约 120.136 秒后发生 `ReadTimeout`，位置是第 7 题的 generation_or_fallback，请求 max_tokens=8192。网络辅助脚本设置了 `timeout=120`，控制器外层等待 160 秒。可以确认客户端在 120 秒读取窗口内没有获得完整回执；无法据此确定服务端是否完成或计费，也没有证据把这次超时计为 8192 输出截断。本地等待窗口与既有 120 秒服务时限口径没有留出传输余量，是本次辅助执行器需要修正的设计。

| 指标 | 实际结果 |
| --- | --- |
| 累计真实 HTTP 尝试（含最初大小写事故） | 38 |
| 成功返回回执 | 37 |
| 用量未知的超时请求 | 1 |
| 已知 prompt / completion / total token | 18,271 / 35,295 / 53,566 |
| 原授权剩余请求 | 2266 |
| 成功回执中的 length | 7/37 |
| 替代批次生成与恢复 | 20 个完整回执，1 次 length |
| 替代批次 verifier | 16 个完整回执，6 次 length |
| 完成题数 | baseline 6；compact_routing 0 |

总 token 至少为 53,566，不能把未知请求记为免费。上述 6 题全是开发集开头的代数题，不具有整套六领域代表性；verifier 的 6/16 截断只是小样本诊断，不能替代完整消融。单题 checkpoint 中 token=0 是该显式文本协议路径未填入 usage，实际成本以逐请求回执为准；不能引用 checkpoint 零值作为成本结论。

原始证据保留在 `outputs/accuracy-20260907-v2/failure-diagnostic.json`、`request-ledger.jsonl`、`model-relay/network-status.json` 与 `model-relay/response-0037.json`。停止后没有继续发出真实请求，也没有把未完成的 `_run/run_summary.json` 改成 completed。

## 离线判分缺陷与修复

旧判分器对这 6 题给出 4 unknown / 2 wrong。核对发现两个 wrong 都是误判：一题的正/负根数用中文等价表达，另一题以 `±` 简写列出可能有理根。运行时 canonical 把逗号分隔的任意文字拆成多项，字面不同直接当作不等；离线 judge 无条件接受此 False。相关检查还发现多项顺序交换可误判正确，以及集合重复元素可误判错误。

仅修改 `evaluation/judge.py`：先限制答案长度，再独立处理集合/多项答案；显式、平坦、精确数值有限集合使用真正集合语义，其它非同形的文字/符号多项结果保留 unknown，不猜语义、不用子串命中、不进入递归或符号解析。`answer_equivalence.py` 与正式聚合策略均未修改，不能称其全部历史语义风险已经修复。

新增固定反例与有界性质检查。包含原有用例的判分专项在旧实现为 11 failed / 11 passed，修复后相关 137 项通过；集合性质同时校验精确分数变形、重排、重复元素、增加元素和比较对称性。

完整离线验收：Python 3.12 的 formal 为 522 passed / 33.82 秒；Python 3.10 为 522 passed / 40.45 秒，保持原 60 秒硬门限。Ruff、53 个仓库源文件/测试编译、few-shot 零 API 预检通过；编译针对仓库文件清单，未将不可读的历史缓存目录算作源文件检查。全量通过仍不代表真实模型提分。

保留旧评分文件，另存 `outputs/accuracy-20260907-recovery/partial-rescore.json`，新自动判分为 **6 unknown**。另存 `assistant-math-review.json`，逐题核对不等式、符号变号、排除分母零点、有理根定理、增长模型和配方后，6 题答案均成立；这是一次 assistant 数学审核，不是双人盲审或完整能力成绩，未覆盖自动判分结果。

新 Q1 代码摘要为 `fab3c040e9e16371c61a1ac0c11d14e3f96978e1f74618dac4b363c1ea2020c1`，新分析摘要为 `4da0135587c0c9a90635715a550853b5fe5ae07bf77679c020ca69dfdb4c508c`。修复前保存各源文件摘要，确认其中只有 judge 改变。旧研究现在被 `stale study or analysis code` 正确拒绝；没有重写旧冻结摘要、删除凭证或混合新旧 checkpoint。

## 已准备的后续执行方案（尚未启动）

`outputs/accuracy-20260907-v3/pilot-plan.json`：SHA `c09c4ebc7fa24027675fcd04c3bba191d77fe58b500edc0e8e81699e286b208d`。新建独立 baseline / compact_routing 首轮配对，各 72 题；原 6 题和失败记录保留作诊断，不拼入新的 Q2 完整运行。仍使用相同 397B、题集、生成源码、模型参数和唯一策略变量。

唯一网络调整是连接等待 10 秒、读取等待 180 秒、控制器等待 230 秒；不改 8192、不加重试、不放开跳转。累计已用 38 次计入原授权，新批次最多 2266 次请求、请求输出上界 18,693,120 token；由于剩余额度少于 144×16 的最坏情形，耗尽时仍停止，不保证能完成最坏分支。已在零网络条件下验证 13 项预算/失败停止检查、成功/超时/重定向三类单次发送，以及未取得重开决定时无法创建网络队列或执行标记。

原方案明确 `stop_after_uncertain_transport_error=true`、`automatic_resume=false`。本次改变中止后的安排并重新解完整 144 次，需要用户明确同意重开；不是再次申请原来已经批准的总请求额度。批准前缺少 `restart-approval.json`，不会联网。延长本地等待不承诺消除服务端超时，失败请求的历史未知用量继续保留。

### 用户批准后的实际配置

用户明确回复：“同意，最多延长至360秒，在此范围内你自己把握”。据此选择连接等待 10 秒、HTTP 读取等待 300 秒、控制器等待 350 秒；参数均在授权范围内。上述 180/230 秒方案、脚本和预检记录先保存为 `.before-restart-approval` 历史文件，再冻结新执行方案，SHA 为 `5f0860381de52d87aaeeb90af79151abc1daac4f5d30cda2384dfda8c54b8f33`。研究协议、题集、代码/分析摘要和策略参数未变。

新 `restart-approval.json` 绑定用户原话、实际等待值、新旧方案摘要和剩余请求额度。13 项预算自测与成功/读取超时/重定向三类单次发送检查再次通过，均为零 API。联网进程通过自动审批后先进入零请求就绪，离线控制器登记两组凭证后启动 baseline。实际进度以 v3 的 `execution-status.json` 和逐请求回执为准；正在运行不代表已取得完整结果。累计成本需加上前两批已知 53,566 token 和一笔未知请求。

### v3 运行中发现（现已暂停，完整配对仍待完成）

只做旁路审核，不修改冻结的求解代码、配置或自动判分；以下不能代替整套结果或归因到正式隐藏集。

- 长等待已有正证据：请求 37 在约 221.953 秒返回 length，completion=8192；请求 39 在约 239 秒返回 stop，completion=7935；另一次正常返回约 271 秒。旧 120 秒窗口会在这些回执返回前结束等待，延长窗口确有本地作用，但不能消除输出截断。
- 多行最终答案被丢弃：开发集第 8 题七个方程、第 13 题九项曲线分析，分别在请求 45、78 得到 finish_reason=stop 且数学内容完整的答案；答案标记单独占一行，后续为编号列表。`_extract_answer` 只接受同一行答案，返回空串；各自三次 512 token 恢复全部 length，实际最终交付均为“未解出”。通用双小题的零 API 反例同样复现，不将未选中的正确候选计为系统正确。证据在 `multiline-answer-loss.json`、`assistant-review.json` 及原始响应。
- 保留候选既有成功也有能力边界：第 7 题耗时 620719 ms，触发 `verify_budget_exhausted` 后仍保留完整的正确参数化答案；第 15 题耗时 630125 ms，同样保留完整候选，但该候选导数错误。在原函数定义域内取 `x=pi/4`，正确导数为 `-124/5`，交付式为 `-64/5`，差异可精确核验。候选完整不等于数学正确，耗时过长导致没有剩余验证时间值得后续独立实验。
- 提取边界的通用零 API 检查还复现：答案标记与第一小题同一行、第二小题另起一行时只保留第一小题；较早有答案、最后答案标记为空时仍返回旧答案；较早有 boxed 中间值、最后为多行答案块时可能取中间值。`extraction-boundary-audit.json` 保存合成样例和实际输出。这些是提取器的离线反例，不代表每种情况已在本轮题目中发生；尚未修改运行时。
- 加粗标记污染答案：第 38 题选中的响应 220 使用 `**最终答案：**`，提取器把结束加粗的 `**` 带入答案，幂运算归一化随后将其改成开头的 `^`。通用零 API 反例 `**最终答案：** 7` 得到 `^ 7`；正常 `x**2` 仍应转为 `x^2`，后续不能靠全局删除星号修复。该题最终行仍有一条完整正确的等价积分，但首条表达式已被污染，数学诊断保守记 unknown，不擅自清理后计对；未选中的干净候选也不补算。证据在 `bold-marker-corruption.json`。
- 工具提示与当前调用能力不一致：本轮 `local_tools=false`，公开文本路径仍发送“请调用工具验证关键计算”。已观察到模型只输出工具调用文本或声称将验证的响应，以及恢复请求。文本中的代码块或“工具确认”不能当作实际执行证据；本轮 trace 的工具调用数为零。后续可独立比较与实际能力一致的提示，不为此恢复私有 client 或扩展参数。
- 最终行遗漏条件：第 18 题的摆线曲率，完整推理已给出全局形式 `1/(40*abs(sin(t/2)))`，并讨论首拱范围；模型原始最终行和实际交付最终行均为没有绝对值的形式。在 `t=3*pi`，该最终式为负，几何曲率为正。按本地最终行口径记错并保留“完整推理含正确形式”的差异；不能说是提取器删掉了绝对值，也不能假定读取全文的官方判分一定相同。
- 第 30 题进一步证明工具提示问题不限于工具候选：请求 172、174、176 全部 `finish_reason=stop`，返回的均为 JSON/文本工具调用；176 来自普通候选，但领域系统提示仍要求工具验证。三次 512 token 恢复全部 length，最终“未解出”。此题没有 8192 生成截断，不应把全部失败归因于输出上限；工具参数里有正确积分式也不能倒算为完成答题。
- 参考标签错误：第 28 题 `73de1a56-66c6-4eac-929a-6307d780e875` 的参考式在对数内使用 `sqrt(1+(5x+1)^2)`，应为 `sqrt(5+(5x+1)^2)`。在 `x=-1/5`，原被积函数和模型交付式的导数均为 `9/(5*sqrt(5))`，参考式导数为 `9/5`。独立推导、冻结标签摘要和原始参考存于 `reference-label-issue-28.json`；冻结题集、自动评分和原比较口径均不修改，数学审核单独披露该错误。
- 工具调用文本不能直接补成答案：第 43 题请求 254 的文本参数把整个分母立方，表达式为 `15/(8*exp(t)*(cos(3*t)-3*sin(3*t)))**3`。独立求导应为 `15/(8*exp(t)*(cos(3*t)-3*sin(3*t))**3)`；取 `t=0`，前者为 `15/512`，后者为 `15/8`。此题实际没有交付答案，不能因为工具参数看似接近正确式就补算正确。没有执行模型提供的表达式。
- 后续开发题预核算发现第 66 题 `a29dedad-3c66-4fb6-a142-1d724cd3f3f0` 的标签问题：级数 `sum(p^(n^2)/2^n)` 对实数参数的收敛集为 `[-1,1]`，参考 `p<=1` 包含反例 `p=-2`，其项的绝对值为 `2^(n^2-n)`，不趋于零。题面未限定非负参数。原始标签和反例保存在 `reference-label-issue-66.json`；这是在读取该题本轮模型答案前完成的标签核验，不是该题已完成或已判错的声明。
- 第 72 题“最少项数”存在口径差异：参考 `(10,31)` 对应保守选择 `1/(n+1)^2<epsilon`；按非严格余项上界可取 `(9,31)`；按实际绝对误差的字面最小项数则为 `(7,22)`。使用 1000 项精确分数部分和加交错级数余项界，确认第 6/7 项误差分别大于 `0.011633`、小于 `0.008775`，第 21/22 项分别大于 `0.001079`、小于 `0.000987`。审核时区分这些解释，不改冻结标签；独立推导保存在 `independent-prechecks-61-72.json`。
- 正常结束也可能没有可用正文：请求 269、288 的回执均为 `stop`，completion 分别为 1655、3256，但辅助执行器保存的正文都只有两个换行。它们不是读取超时，也不是 `length` 截断。辅助执行器在没有原生 `tool_calls` 时只保存 `message.content`，没有保存原始完整 HTTP JSON，因此无法核实当时是否还有 `reasoning_content` 或其他字段，更不能确认缺失答案的服务端原因。证据在 `empty-content-stop.json`；这只描述本地观测，不能直接归因到正式平台。本轮不为补日志而修改冻结执行器。
- 完整符号恢复答案被二次提取丢弃：`_quick_fallback` 返回已提取的答案本体，`_solve_tools` 将其直接作为候选；后续 `_answer_for_aggregation` 再次提取，只认可裸数字或带标记的公式。零 API 重放确认：恢复响应 `最终答案：sqrt(2)` 第一次提取得到 `sqrt(2)`，第二次为空并被标为 `truncated_isolated`；数字对照 `7` 正常。第 51 题实际请求 301 的 `stop` 响应 `最终答案：\tan^2(\alpha)` 同样复现。该题仍由后续完整普通候选答对，不能说这次丢弃已造成该题未解出。全部候选无效后的最终兜底直接构建答案，不经过同一二次提取路径，需区分。证据在 `fallback-marker-loss.json`；后续应保留经过验证的恢复答案结构，不能通过放宽所有无标记残句的接受规则来修复。本轮尚未修改运行时。
- 最终公式正确不代表正文核验正确：第 64 题的最终导数级数通项正确，但正文将 `x^6` 系数写成 `1/14400`，并声称与通项匹配；通项实际给出 `7/9!=1/51840`。数学审核按最终公式记 correct，同时保留这处实质性错误；模型声称的工具输出没有实际执行证据，不能据此宣称推理与核验全对。
- 配对组第 8 题再次未交付答案，说明仅简化路由提示没有消除提取边界问题。请求 469、471、473 均正常结束且七个方程的解全部正确，但最终答案分别使用换行编号列表、无冒号标题、`**最终答案**：`；原提取器零 API 重放对三者均返回空串。三个 512 token 恢复均 length，实际最终交付为“未解出”。`candidate-answer-marker-loss-8.json` 保存原响应摘要与最小格式反例；不能将未保留的三个正确回答补算为本题正确。

`assistant-review.json` 保存每条实际交付答案、checkpoint SHA 与单次 assistant 数学审核依据，明确区分 correct、wrong、no_answer、unknown，并记录适用假设。它不是双人盲审，也不覆盖 Q0 自动四态评分。完成两组前不计算稳定提分、不修改默认策略。

### 已完成的 baseline 首轮（配对组现已暂停）

基线 72 题全部完成并由控制器登记为 `dev:baseline:0`。运行 ID 为 `61eecc20b96742909899658de0cdad93`，输出摘要 `010f79740511ff5d613e9889dbe2a98f7e4cf18e4d38ff903051445f5c6a5128`。重新计算绑定评分及请求凭证均与已保存文件一致；实际 429 次请求、1,000,579 token、约 5 小时 17 分钟，178 次 length。此处成本仅为 v3 基线，不含历史批次或正在运行的配对组。

| 请求阶段 / 上限 | 请求数 | length 次数 | 实际 total token |
| --- | ---: | ---: | ---: |
| 生成 / 8192 | 216 | 23 | 730,187 |
| 短回复恢复 / 512 | 91 | 82 | 63,608 |
| 校验 / 1024 | 121 | 72 | 204,344 |
| 批评 / 1024 | 1 | 1 | 2,440 |

冻结自动评分为 **0 correct、0 wrong、60 unknown、12 error、0 missing**。12 个 error 对应未交付答案，并非 12 次网络故障；本组所有请求均有完整回执。自动评分没有识别出任何确定的对错，不能把其保守下界 `0%` 当作模型正确率。其 `truncated_items=68` 来自流程事件，不能与 8192 的实际 length 次数混用；本地 `invalid_items=0` 也不抹去 12 道未交付题，更不等同于正式平台判分口径。

单次 assistant 逐题数学诊断为 **56 correct、2 wrong、12 no_answer、2 unknown**，均绑定实际 checkpoint。wrong 是第 15 题导数错误和第 18 题最终曲率式遗漏绝对值；unknown 是第 38 题加粗标记污染，以及第 49 题最终行包含复根、参考只列实根而题面未明示数域。该诊断采用实际最终答案口径并保留每题假设，不声称全文推导全部正确，也不是自动分数覆盖或双人盲审。第 72 题两次 8192 生成与三次 512 恢复均 length，另一次普通候选 stop 于工具文本，最终未交付；参考项数口径的歧义不影响这一交付事实。

基线校验最终状态为 48 次 stop/pass、1 次 stop/fail、72 次 length/unknown。唯一 fail 为请求 206、第 35 题的某个候选；其正文曾出现错误数值，末尾已给出正确公式和数值。单字判决没有说明原因，不能据此确定它纠正了数学错误或获得净收益。

`baseline-audited.json` 保存本组绑定复核与阶段统计。控制器随后开始同一冻结题集的 `compact_routing`，现已按用户要求暂停；本次两组比较仍未完成，不据基线单组选择或晋级策略。

按已审核的实际交付结果分组，12 道未解出题消耗 71 次请求、216,924 token，占本组 token 约 21.7%；另有 56 道正确题消耗 701,445 token，2 道错误题 39,127 token，2 道待定题 43,083 token，合计与凭证一致。这是定位故障成本的事后诊断，不能推断修复格式后 12 道题都会转为正确。

### 用户要求暂停后的最终状态

用户原话：“现在是又在跑第二轮？如果是先暂停，太慢了”。运行中的 `compact_routing` 是同一首轮里的第二种策略，并未开始第二次重复。已停止离线控制器、写入网络停止标记；网络进程收齐暂停前已发送的请求 494 后正常退出。两个执行进程及监测均已结束，不自动续跑。

基线完成 72/72，对照组完成 11/72，第 12 题未完成，不算已交付或缺答案的完成题。对照组 11 题单次 assistant 数学复核为 10 correct、1 no_answer；与基线同一 11 题均一致，未观察到这部分题目的正确率变化。全部 83 份已完成答案已绑定 checkpoint 审核，但完整配对矩阵仍为 false；自动评分和人工盲审边界不变。

v3 共 494 次请求、494 份回执、1,099,952 token；加上先前批次，累计 532 次尝试、已知 1,153,518 token，仍保留历史一笔未知用量。请求 494 在控制器终止后返回，外部收尾依据网络进程保存的响应及文件时间补记成本，明确标注 `external_user_pause_reconciliation`；不将其当作未完成题目的最终交付。原控制器账本、状态、部分组摘要保存为 `.before-user-pause` 文件，暂停事实、用户指令、完整回执校验和部分结果分别存入 `user-pause-receipt.json`、`audited-result.json`。仅完整基线登记为 Q2 运行；没有晋级、改默认、提交或推送。

## 判分与声明边界

完整组应使用 Q0 绑定评分与配对比较，保留 correct/wrong/unknown/no_answer/error/missing 和 invalid。unknown 不人工猜成正确，真实双人盲审仍单独待补。失败研究不进入新研究的准确率统计，但其成本计入本次实际消耗。

本轮首个重复的配对未完成；Q2 完整晋级仍需重复矩阵和独立留出证据，不自动 select、accept、改默认策略、提交或推送。本地输出也不等于正式 `0641043` 的 26/112，因为两份源码关系尚未核实。完整比较当前不存在；v2 已停止，v3 也已按用户要求暂停，分别保留证据和成本，不自动续跑。
