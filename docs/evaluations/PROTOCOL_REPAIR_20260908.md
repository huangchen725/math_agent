# 2026-09-08 参数传输与严格回放修复

本轮按用户确认的方案实施：保留 `5c2f7a0` 的正式求解代码，修复离线中继与回放的参数语义，补充定向回归并同步证据。没有真实 API、推送或正式提交；准确率收益仍未验证。

## 已核验的版本关系

来源为用户提供的 [GitCode 仓库](https://gitcode.com/ZYM725/math-agent-xh202627-v3)，通过完整 Git 对象比对，不以合并说明代替源码。

- 被测提交 `06410436b0fc5ae18830a18fed4745681a7966f2` 的父提交是 `a27634e3e57cc6587f067ee8c666f43555b4e6fe`、`dfb5ce8aba29aff6a2f99902c3e19d9c8cc4146d`；正式 `_chat` 确实显式关闭思考。
- `0641043 + 841fc35 → 870a4cfe19ac4bdf7df4c7bc46123b5c0e8089a9 → 5c2f7a0fdd7d7ae0d921bc0964b814b150540c2d`。
- `0641043 → 5c2f7a0` 的正式入口为 +114/-16 行答案交付修改；四个正式依赖、AgentConfig、提示及思考开关不变。
- `841fc35 → 5c2f7a0` 的正式路径实质差异是 `thinking_mode=False`，整个仓库另有本地工具、适配器、离线工作流与测试修改。
- 本地从干净 `841fc35` 快进到 `5c2f7a0` 后，再单独实施本轮修复，保留可审计的合并边界。正式源文件不作进一步修改。

MAT-013 的 26/112、0 invalid、77/803 截断只属于 `0641043`。源码证明请求参数的设置，不替代服务端转发或实际模型回执。不能保证新版本达到 23.21%，也不能断言省略思考参数的所有版本必定约 9%。公开文档依据及历史三参数推断纠正见登记册 MAT-014。

## 缺陷与修复

| 缺陷 | 修复行为 | 验证 |
| --- | --- | --- |
| RelayClient 接收 False 却丢弃，最终 HTTP 无思考字段 | `_payload` 保留显式布尔；排队、准入、请求指纹和 HTTP 重新校验均保留；缺失不改写，非布尔拒绝 | 端到端模拟 HTTP 的缺失/False/True 三种请求、队列与 ledger 指纹一致性；90 组有界 JSON 往返性质 |
| 文本中继/观察器静默丢弃工具设置 | 非 None 的 tools/tool_choice 在派发前报错，不自动扩展为工具执行 | 队列和发送次数保持不变；ObservedTextClient 转发 False 并拒绝工具参数 |
| 回放只比较原三键，False 可复用 True 或缺失字段的录制 | 比较完整支持的请求键和值；缺失、null、False、True 区分；未知生成参数拒绝；分叉不消耗录制 | 四种思考状态全组合、工具参数改变、未知字段以及真实 Agent + fake 录制的流程回归 |
| 新队列封装不能被旧平铺录制读取器识别 | 同时读取 `{ordinal, payload}` 和历史平铺请求；新快照 schema 2 的请求摘要覆盖完整参数 | 两种格式的 capture/compare、原件篡改检测、思考字段指纹 |
| 旧规则仍写三参数且夸大跨批次因果 | 同步 AGENTS、人工协议、机器版本、维护技能、架构、规范、报告；严格正式 fake 只接受四个 keyword-only 参数 | policy guard、严格调用测试及完整正式闭包/污染/隔离回归 |

`compare` 默认只做解析诊断，输出 `replay_contract=parser_only`；`--solve` 为 `exact_recorded_parameters`。旧快照仍可读取，不补写原录制的思考字段；请求遗漏和显式 False 不等价。严格匹配仅证明已记录请求参数一致，不证明随机模型重跑会产生相同回答，也不产生新的正确率。

中继控制源码已变化，旧计划的 controller 指纹失效；不自动迁移或恢复已暂停实测。未来真实实验须使用新计划/新目录和明确的请求参数、重新取得额度授权，原始证据继续只读。

## 验收记录

- 原 `5c2f7a0` 上新增回归得到 27 failed / 42 passed；失败包含最终 HTTP 缺字段、不同思考设置仍匹配、新队列封装 KeyError。
- 修复后中继与回放 69 项通过；补充队列封装、Agent 流程与 Q2 观察器测试后，定向 115 项通过。
- Python 3.12.14：完整 `--formal` **666 passed，42.89 秒**；Python 3.10.11：完整 `--formal` **666 passed，50.91 秒**，均在原 60 秒门禁内完成。
- Ruff、60 个 tracked/new Python 文件的 Python 3.10 语法编译、`git diff --check` 通过；few-shot 离线预检解析 21 项，零 API。
- 494 份原实测响应的解析诊断仍为 478 unchanged / 13 newly_extractable / 2 changed / 1 no_longer_extractable。83 道严格回放全部因旧请求未记录思考开关而在首个请求分叉，消耗旧响应 0 条；这属于预期拒绝，不是 83 道数学解答失败。
- `5c2f7a0` 的正式导入闭包经 Git diff 核对完全不变，源码指纹与两版门禁日志保存在被忽略的 `outputs/protocol-repair-20260908/`。原实测文件逐份哈希复查不变。
- 已将中继、回放和答案交付回归文件纳入完整门禁的必需文件清单，缺失时阻断。

离线通过说明已覆盖的确定性行为成立；完整 Q2 能力验收、独立双人复核和正式成绩仍分别待补。
