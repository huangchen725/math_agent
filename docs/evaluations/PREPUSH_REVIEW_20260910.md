# 提交前全面复核（2026-09-10）

用户要求全面检查，随后自行提交推送。复核起点为本地dcbe4d03d2dd15aa6ef8daf64feb64c47d9c79d5，分支work/b1-b2-offline-20260908，开始时工作区干净。本轮不调用模型API、不推送、不触发正式评测。

## 审查发现与修补

发现并修复一项P2：专用快答协议对自由文本字段的冲突过滤不一致。

- CONDITIONS内写MATCH: NO或Markdown包装的VERDICT: B，末尾仍写YES/A时，旧实现仍可提前接受。
- 最终答案行写“3. 答案：2”时，通用答案提取器会只取后一个2，丢弃前面的冲突。隐藏控制符也可随被丢弃的前缀绕过检查。

现在CONDITIONS、CHECK和最终答案体共用禁止嵌套字段、围栏与隐藏控制符的规则；末行只取精确前缀后的完整body并校验，不再使用通用提取器。32项新增回归覆盖两说明字段的12种变体、原预算内回退及7种答案体冲突。先在旧实现复现条件字段13项失败，再完成修补；没有改变模型提示、四参数False、预算、正式依赖或语料。

DOC-001曾因运行时修改缺少配套说明阻断changed门禁；随后同步README、ARCHITECTURE和工程规范，旧阻断不冒充通过。首日报告与其1114项验收、91次真实API数据保持原样，本报告单独绑定修补后的源码。

另发现一项本地诊断工具的P2：Windows下读execution.json的句柄可能阻止原子替换，Admission.stop虽已将内存状态设为stopped，首次写盘仍会PermissionError；原并发测试因此得到witnessed=[False,False]。已用真实文件句柄与完整confirmation fixture的事件屏障确定性复现，排除旧pyc。修补由helper.read/write共用同进程RLock，仅保护短文件读取和临时文件写入/替换，JSON编解码、网络与等待留在锁外。没有新增API重试，也未修改原3秒停止时序断言；新增确定性并发回归。该helper不在正式求解闭包中，旧91次请求继续绑定原已冻结执行工具，不能据新helper更新历史实测声明。

## 重新验收

修补前两种Python各1114项全量检查通过。仅解析修补后的1146项诊断全部通过（52.60秒），但formal的Py3.12与Py3.10先后触及60秒门限；中间Py3.12一次58.86秒通过，未据此忽略其他失败。旧日志均保留。

针对现有离线Q0去重热点，增加每次调用临时的一元位编码：每个字符使用独立位区，两个编码按位交集的1数精确等于原Counter多重集交集；超过65536位时在移位前回退原Counter逻辑。原0.88浮点比较和最终双向near_template完全保留，无跨调用缓存、不减少文件重读或哈希。新增4761对独立交集等价验证、大小上限回退及原判断调用集合测试；相关67项专项通过。正式求解闭包未导入该离线实现。

独立无profile局部计时：公开Q0开发题函数Py3.12约4.65倍、Py3.10约3.72倍加速；同形状40题fixture约6.23倍、5.01倍。它不直接推算全量秒数。相同单项Q2测试的profile内去重246次从3.000秒降至0.248秒，read_bytes仍6873次；profile会放大Python循环差异，总耗时变化不全部归因于此。全量仍按原60秒门限重新验收。

Py3.10随后完整诊断为1148通过、上述并发测试1失败，64.82秒；保留于pytest-py310-durations.txt，并据此修补真实并发缺陷。单项Q2在仓库输出目录的独立临时目录下完成5.72秒（测试调用5.29秒）；此前系统临时目录全量运行中的同项为9.94秒，不能把不同运行的全部时间差归因于代码优化。

最终两种解释器的原60秒完整formal均通过：Python3.10.11为1150项、54.54秒（CLI55.41秒）；Python3.12.14为1150项、41.42秒（CLI42.61秒）。Ruff与所有81份发布Python源的编译通过，验证期间Python/资源字节不变。全部旧超时与并发失败日志保留，不宣称后续任意机器或负载都有相同耗时。

最终测试使用工作区临时目录。PYTEST_ADDOPTS中的Windows反斜杠被参数拆分移除，实际产生了仓库根下两个kamimath-agentoutputs开头的测试目录；测试结束后已核验绝对路径并整体移入outputs/prepush-20260910/executed-temp-py310和executed-temp-py312，保留原件且不进入提交。Py3.12编译回执额外包括73份前轮临时fixture源码；81份发布源码与525份资源的摘要在双解释器回执中完全一致，最终包仅使用确认的版本化文件范围。后续临时目录选项应使用正斜杠路径。

最终运行时user_agent.py摘要为0bf98622ec3260a276eefd6120a6d6e75460b9ee0259c6adbc5fca645cf25a25；本地helper摘要为12c732792b1f87b4eca1aa0d2bdd7eb4142020f9f8e0804788ec3ad4c7a067d1。正式源相对dcbe4d0仅快答解析函数改变，客户端/配置/加载器AST不变；语料源与全部资源未改。

38条未命中路线历史请求严格四参数回放通过，6份最终答复字节一致；同8份快答确认回放仍4次接受，全部答复一致。双Python×四种资源状态的8个隔离发布探针通过，15个预加载同名模块未被覆盖，客户端未被私有探测。上述均为零新API证据，不是新模型成绩。

详细机器回执位于outputs/prepush-20260910/，完整门禁原始日志及指纹位于outputs/day1-20260910/validation-*-prepush-accepted.json；首日旧证据不覆盖。最终包及文件摘要另见package-final-receipt.json，汇总见acceptance.json。

## 证据边界与推送

实际已提交的683文件包、1054条数据与代码固定摘要在独立只读审查中一致。修补后工作区版本另行打包验收，不能用旧dcbe4d0包代表新源码。数学正确率、未知正式题命中率和平台实际运行仍需要正式结果；本地全量通过不代表35%或绝对零分风险保证。

本次修补共10个代码、测试及文档文件，留在工作区，由用户提交后推送；不只推送旧dcbe4d0。当前为work分支，仅推送该分支不会更新main；若平台抓取main，须将含本次修补的版本合并至main。GitCode按用户要求未操作。本次未发现尚未修复的本地发布阻断项。
