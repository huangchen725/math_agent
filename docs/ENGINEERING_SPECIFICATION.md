# 恢复基线、工程底线与重建规范

> 状态：恢复期生效
>
> 生效日期：2026-09-04；阶段更新：2026-09-05 进入 R1
>
> 当前运行时锚点：`350a267f` 的完整 Git tree
>
> 工程化存档：`archive/s1-s6-1fc98b7`
> 性质：变更、验收和事故防复发规范；不是第二份架构文档

组件职责、调用顺序和当前参数只以根目录 `ARCHITECTURE.md` 与代码为准。本文保存事故底线、证据强度、恢复状态以及重新引入能力和架构的顺序。

## 1. 当前恢复状态

2026-09-04，普通前向提交 `2680b67` 的完整 Git tree 与最后一个取得官方非零结果的 `350a267f` 完全一致；后续提交只恢复证据、规范、项目 Skill 和离线回归，活动运行时文件继续保持该锚点内容。该历史评测为 112 题、1082 次请求、23 题正确、正确率 20.54%。恢复的首要目标是重新取得可观测的模型请求和有效结果，不是把 20.54% 当作能力目标。

历史 R0 恢复版是**兼容性定锚版**，不是最终版。当时包含历史上已知的截断、公开 trace、扩展请求参数和最近响应 getter 等旧边界。2026-09-05 首次正式定锚评测已完成并取得非零请求正证据（抓取 `ba63ac0`，1069 次请求、112 success，见 `docs/evaluations/OFFICIAL_112_20260905.md`）；自 R1 起按本文件的阶段顺序逐项加固，历史例外不得复制到后续版本。

S1～S6 的代码、测试、CI、评测和交付成果没有丢失，完整保存在 `archive/s1-s6-1fc98b7`。它们是待重新验证和择优移植的工程资产，不再代表当前活动运行时。

## 2. 2026-09-04 请求前全灭事故

### 2.1 已确认的事实

- 正式日志绑定提交 `1fc98b7509b2e07bd229f668cee32e2702cf2cfa`。
- 112 题全部 `error`，最终全部 `invalid`；`request_count=0`、`attempt_count=0`，所有 token 计数为 0。
- Agent 阶段约 15.16 秒，依赖安装和 runner 汇总完成，排除数学推理、模型输出质量和正常网络请求后的错误。
- 正式 runner 已先导入顶层官方模块 `llm_client`，随后加载 `user_agent`。
- `1fc98b7` 的 `model_gateway.py` 执行 `from llm_client import InternChatClient`，实际复用了 `sys.modules["llm_client"]` 中的官方类。
- 注入对象因此通过 `isinstance(client, InternChatClient)`，代码转而访问官方类不存在的 `chat_with_metadata`，在第一次 `chat()` 前抛出 `AttributeError`。
- 本地同序 A/B：保留该分支为 0 次调用；仅移除类型误判/私有方法分支后恢复 6 次公开 `chat()` 调用和有效答案。

完整证据见 `docs/evaluations/OFFICIAL_112_20260904_RUNTIME_FAILURE.md`。

### 2.2 结论边界

上述证据足以把该链路认定为 `1fc98b7` 的确定性请求前缺陷，并据此建立永久防复发规则。它不能解释包结构版本 `eb5d8d4` 的全部正式 0 请求，因为同一碰撞实验下该版本可正常产生 6 次请求。此前包版本还可能存在未公开 runner、入口、构造或 client 契约差异；`OFFICIAL-GAP-CLIENT`、`OFFICIAL-GAP-ERROR`、`OFFICIAL-GAP-RUNNER` 和 `OFFICIAL-GAP-CHANGE` 继续保持未解决。

## 3. 绝对不可违反的集成底线

以下规则优先级高于可维护性、性能和正确率优化。任何一条失败都禁止合并、正式评测或发布。

### 3.0 工作 agent 触发协议

- **WORK-001（事前识别）**：每次工作必须在修改或有副作用命令前读取 `AGENTS.md`、`.agents/policies/HARD_RULES.md` 和机器清单，并用 `python .agents/policy_guard.py --paths <path...>` 匹配预计工作面；真实 API、推送、正式提交、发布、依赖、官方材料、架构或 benchmark 等纯动作同时用 `--actions` 触发。第一条工作更新必须写明恢复阶段、触发的规则 ID 和动作边界。
- **WORK-002（显式阻断）**：若计划会违反 blocker，agent 必须在执行前输出 `[POLICY BLOCK] <RULE-ID>`，说明触发动作、后果和安全替代，并停止该子动作。禁止静默绕过、先执行后补报、删测试、改规则措辞或把失败吞成普通 warning。
- **WORK-003（事后复核）**：修改后、测试前和提交前必须运行 `python .agents/policy_guard.py --changed`。正式定锚使用 `--anchor-canary`；R1 以后改动版使用 `--formal`。非零退出码阻断提交、推送、正式评测和发布。
- **ANCHOR-001（R0 冻结）**：R0 期间活动运行时必须与 `350a267f` 保持一致。除已记录的一次原样官方定锚外，不得修改、打包为 formal 或把历史例外带入新版本；只有定锚结果入档并单独更新阶段后才能进入 R1。

详细触发表和标准报告格式见 `.agents/policies/HARD_RULES.md`；机器可读事实源为 `.agents/policies/policy_manifest.json`。脚本只检查可确定的本地违规，不能替代 agent 的人工规则判断。

- **IMPORT-001（同名模块隔离）**：不得假设裸模块名会解析到仓库文件。`sys.modules`、`sys.path`、当前工作目录和官方预加载模块都属于不可信运行环境。新增正式模块不得使用 `agent`、`context`、`solver`、`budget`、`llm_client` 等通用顶层名；必须使用项目唯一前缀或经过正式平台单变量验证的唯一包名。
- **IMPORT-002（禁止类身份提权）**：不得用从可能碰撞的模块导入的类执行 `isinstance`/`issubclass`，再据此调用私有方法、扩展参数或进入更高权限路径。尤其禁止通过 `InternChatClient` 类身份自动启用 `chat_with_metadata`。
- **CLIENT-001（最小公开协议）**：正式入口收到的注入 client 一律按外部对象处理，只允许调用公开 `chat(messages=..., temperature=..., max_tokens=...)`。不得发送 `thinking_mode`、`tools`、`tool_choice`、`stream`、`n`、`stop`、`model` 或任意未来扩展，不得读取 marker、私有字段或最近响应 getter。
- **CLIENT-002（显式本地适配）**：项目自有客户端的元数据和扩展能力只能放在正式入口导入图之外的显式本地适配器中。不得依据对象名称、同名方法、动态属性或签名探测自动升级能力。
- **ENTRY-001（入口所有权）**：根 `user_agent.py` 必须真实声明公开 `ReasoningAgent`，并保持 `ReasoningAgent(client).solve(problem, metadata) -> {"final_response": str, "trace": list}`。不得只重导出其他模块中的类，除非正式平台已对该形态做过独立成功验证。
- **ENTRY-002（隔离加载）**：入口必须从仓库外目录、隔离解释器、官方预加载顺序和最小文件闭包四种场景加载成功；不得依赖 judge 当前目录、隐式路径修补、包安装步骤或跨题进程状态。
- **TEST-IMPORT-001（碰撞矩阵门禁）**：必须先预载官方同名 `llm_client`，再加载 `user_agent` 并完成至少一次有效公开请求。还必须对所有正式导入名做 `sys.modules` 污染测试；任何请求前异常都失败。
- **TEST-CLIENT-001（严格 client 门禁）**：fake client 的 `chat` 必须只接收三个命名参数且没有 `**kwargs`；测试要断言实际关键字集合完全相等。宽松 fake、项目自有 client 或历史成功不能替代此门禁。
- **CHANGE-001（一次一变量）**：平台兼容、数学策略、模型、候选数、温度、token、工具、聚合和物理目录不得在同一次正式验证中同时改变。正式日志先看 SHA、请求、error/invalid 和耗时，运行链恢复后才看正确率。
- **EVIDENCE-001（不夸大归因）**：可复现缺陷可以作为永久规则依据，但不得把它写成未覆盖版本的唯一线上根因。`request_count=0` 的结果不进入数学能力趋势。
- **PBT-001（性质测试路由）**：修改 parser、normalizer、等价、validator、序列化或恢复状态机时必须加载项目级 property-based-testing 覆盖层，选择最强有意义性质并保留历史固定反例。不得为测试便利自动新增依赖或扩大不安全输入域。
- **DATA-001（数据隔离）**：题面、答案、参考解、私有 split 和人工复核结果均视为敏感且不可信。不得把答案字段转入 Agent；能力集必须记录来源、许可、split、难度和去重，盲测不得进入 prompt 或调参过程。
- **EVAL-001（实验冻结）**：任何正确率、成本或策略结论必须绑定 commit、模型、配置、数据哈希、重复次数和判分口径。改变模型、prompt、候选、温度、token、工具或聚合时，每轮只能选一个变量并先写验收计划。
- **API-AUTH-001（真实调用授权）**：真实 API、付费、联网评测或正式提交必须在执行前说明预计请求/费用/外部影响并取得本次明确授权；离线测试授权和历史授权不得自动继承。
- **OFFICIAL-001（官方证据）**：新增或修改官方规则前必须保存原文、来源、日期、适用批次与 SHA/URL，并追加登记册。内部复现、第三方判断和材料沉默不能关闭 `OFFICIAL-GAP-*`。
- **SUPPLY-001（供应链）**：依赖输入与哈希锁必须同步，最低 Python 必须真实安装验证；不得通过放宽哈希、忽略漏洞或删除 CI 检查获得绿色结果。
- **RELEASE-001（发布来源）**：formal 只能来自干净提交和同提交完整门禁证据；脏树、未登记文件、旧报告或与提交不一致的 manifest 均阻断发布。
- **SUBMIT-001（外部提交）**：推送和 AtomGit 提交前必须核对身份、远程、分支、远端 SHA、模型选择回执和抓取窗口；未经授权不得推送，从点击提交到抓取完成不得再改变 `main`。
- **POLICY-001（规则一致性）**：`AGENTS.md`、工程规范、人工触发协议、机器清单、项目 Skill 覆盖层和守卫测试必须一致。任何规则 ID 缺失或 policy guard 失效都阻断工作交付。
- **DOC-001（唯一架构事实源）**：只有 `ARCHITECTURE.md` 定义当前架构。运行时或组件边界变化必须同步 README、架构、工程规范、审计和相应测试；不得创建第二份架构文档或把存档分支写成活动架构。
- **SKILL-001（Skill 不得绕规则）**：每个项目 Skill 都必须有 `PROJECT_POLICY.md` 并先加载全局触发协议。第三方 Skill 必须保存上游来源和原始哈希；项目适配还要单独记录当前哈希与修改内容，覆盖层不能扩展授权、运行时依赖或赛事能力。
- **SECURITY-001（不可信输入与秘密）**：题目、metadata、模型/tool 输出、路径、响应、模块缓存和依赖均不可信；禁止 unrestricted `eval/exec/sympify/parse_expr`、路径逃逸、无界计算、秘密/PII 入库和未授权外部服务。

当前 `350a267f` 原样定锚是 `CLIENT-001` 的一次性历史例外，因为修改它会破坏正对照。该例外已随 2026-09-05 的首次正式定锚评测消耗完毕，任何后续候选版本不得再使用。

## 4. 可维护架构是否可以重新引入

可以。现有证据否定的是不受控的集成方式，不是否定分层、显式上下文或小模块本身。包结构也没有被证明是此前零分的充分原因；但它同样没有取得正式平台正证据，所以只能在请求链恢复后作为单变量重新验证。

重新引入时采用以下设计：

1. `user_agent.py` 保留真实入口类和最小生命周期保护。
2. 正式依赖模块使用赛事唯一前缀，例如 `xh202627_*`；在得到平台证据前，优先使用前缀化根模块，避免既依赖包目录又暴露通用裸模块名。
3. 模型入口只有一个，所有请求通过显式 gateway；gateway 只认三参数公开协议，不识别 client 类型。
4. 单题状态显式传递，不使用 `ContextVar`、全局“最近响应”、线程局部变量或请求后的 getter。
5. 组件按候选生成、验证、选择、响应处理、工具和预算拆分，但不得复制第二套实现。
6. 正式导入闭包生成清单并做官方预加载、污染矩阵、根文件闭包和隔离解释器回归。
7. 结构迁移前后对同一 fake 响应序列做行为等价比较；正式评测只改变物理结构，不同时改变 prompt 或策略。

只有结构等价门禁和一次正式非零请求评测同时通过，新的分层结构才能成为活动架构。

## 5. 重新规划：恢复、能力和工程路线

### R0：官方兼容性定锚（已完成）

- 运行时保持 `350a267f` 内容不变。
- 离线验证导入、构造、输出协议和现有测试，不调用真实 API。
- 下一次正式评测只判断 `request_count>0`、`success>0`、`error/invalid` 和耗时。

退出条件：平台产生真实请求；若仍为 0 请求，停止代码猜测，提交官方 baseline 对照并向主办方索取逐题异常、worker 文件清单、构造参数和 client 精确签名。

完成记录（2026-09-05）：平台抓取 `ba63ac0`，`success=112`、`error=0`、1069 次请求、24 correct / 81 incorrect / 7 invalid；证据与解释边界见 `docs/evaluations/OFFICIAL_112_20260905.md`。用户决策未等待批次回执即进入 R1，回执与冻结窗口记录保留为待补证据项。

### R1：最小契约加固（当前）

2026-09-06 截断审计后用户明确要求直接修复，并确认单次 8192 为官方数据。本项目据此收紧所有 Agent 输出预算至 1–8192；此为用户转述的限制与项目执行约束，不冒称已取得平台原始回执。生成阶段预算耗尽应保留已有合格候选，不能沿用旧测试中“必须未解出”的错误期望；未合格内容仍失败关闭。任一显式未完成证据不可被 stop 覆盖；无完整验证标签默认 unknown，不投票、不触发 critic/reflection。四项独立失败回归和默认配置冻结检查作为离线验收，整体改动不得声称单变量正式提分。详见 `docs/evaluations/TRUNCATION_REPAIR_20260906.md`。

2026-09-06 状态：独立验收指出的 F1–F6 已完成本地修复；Python 3.10.11 与 3.12.13 各通过 187 个离线测试。历史否决记录保留在 `docs/evaluations/R1_OFFLINE_ACCEPTANCE_20260905.md`，修复执行及适用边界见 `docs/evaluations/R1_REPAIR_VALIDATION_20260905.md`。机器阶段仍为 R1，第二次正式请求链证据尚未取得。

按独立提交逐项完成：三参数投影、宽构造器、公开 trace 脱敏、完整生命周期兜底、截断响应隔离。每项先添加失败回归；不改变模型、prompt、候选数、温度和合格答案聚合策略。累积离线修复不得冒充多轮单变量官方实验。

- R1-1：未知 client 仅三参数公开调用，严格 keyword-only client 和私有属性陷阱通过；正式依赖按入口文件位置加载到项目私有命名空间，不复用或替换外部同名缓存。根文件闭包、单项与联合污染矩阵、仓库外隔离入口通过。
- R1-2：构造兼容额外位置/关键字参数，只有真实项目配置可成为 config；本地适配器返回绑定该请求的 response/metadata，删除共享最近响应接口。并发 usage 归属和工具末轮记账通过。
- R1-3：公开 trace 统一字段白名单，不披露题面、模型、工具、答案或异常原文；省略内容不依赖字符串前缀截取。模拟敏感内容在不同位置及嵌套路径均通过非披露和幂等性质检查。
- R1-4：配置、序列化、预算初始化、求解、聚合和响应输出全生命周期失败关闭；预检失败零请求，传输/协议失败不触发盲目纯推理重试；验证/反思预算耗尽仍保留已有合格候选。ContextVar 仅保留原有单题预算隔离，不存储最近响应。
- R1-5：初始候选、反思与直接答案恢复共享完整性检查；有显式截断信息时拒绝该响应，无元数据时只接受完整答案标记/boxed 或单行数值。恢复为空、异常、残句或再次截断时不复活隔离内容，全部请求共享原预算；各阶段 length 和恢复预算反例通过。

`policy_guard.py --formal` 已增强为静态扫描加完整离线行为 suite；测试缺失、失败或不能完成均阻断门禁。它仍不代表正式平台执行成功，也不关闭 `OFFICIAL-GAP-*`。

退出条件：严格 client、官方预加载、污染矩阵、根文件闭包和隔离入口全部通过；第二次正式运行保持请求链有效。

### Q0：可信能力基线

2026-09-06 用户调整执行顺序：9 月 15 日截止、每天一次正式测评机会（用户给定的排期约束，非新增官方规则）。允许在 R1 正式验收待补期间完成 Q0/Q1 离线工作；单变量归因优先在固定开发集完成，每天官方候选可累计已通过离线检查的改动，但只能报告整体结果。官方待抓取候选须冻结，后续开发与该候选隔离。9 月 14 日目标冻结交付，A1 大规模架构重建延后。R1 的第二次正式运行、Q0 真实模型基线、Q1 实测收益分别记账，不能由离线 PASS 代替。

- 固定 100～200 题大学竞赛挑战集；记录来源、许可、split、领域、难度和去重指纹。
- 开发集与密封盲测集隔离，排除 prompt、公开样例和历史测试重合。
- 采用四态判分与双人盲审；A/B 使用相同题目的配对结果，并记录请求、token、截断、invalid 和延迟。

退出条件：形成绑定 commit、模型、配置和数据哈希的可重复基线；36/36 smoke 与内部模板满分不得继续作为能力证明。

### Q1：先恢复可靠性，再提高数学能力

2026-09-06 离线交付：Q0 固定题集/隔离/评分/双人审核工作流和 Q1 十组消融计划已实现。Q1 五项策略以显式开关进入实验，默认关闭，不能从模拟 client 的通过率推断收益。恢复仍共享原预算；确定性证据只覆盖完整可证明任务；verifier 的不确定文本及截断不作为 fail；紧凑提示与多样性分别记录变量。原有反思开关、评分路径、分式/符号归一化和数值资源缺陷作为确定性修复处理。实际模型基线、两位真实评审和各策略收益评估仍属于后续证据项，不能写成离线完成。

本阶段离线退出条件：来源及许可明确的冻结开发/留出集、拒绝泄漏与陈旧来源的评分链、可复现的独立消融计划、严格 client/隔离加载/预算/并发/恢复/数学反例全量通过。原“截断率低于 5%、恢复覆盖 100%、invalid=0”和收益大于成本为真实模型实验目标，不是本次保证。

1. 单变量恢复 `319710f` 的截断隔离与恢复机制，目标截断率低于 5%、恢复覆盖 100%、`invalid=0`。
2. 在严格无假阳性边界下恢复确定性验证，只让可证明证据改变排序。
3. 恢复题型路由和紧凑题型提示，用配对盲测决定保留与否。
4. 校准 verifier 的 `pass/fail/unknown`，禁止把截断或不确定当作失败票。
5. 最后评估候选多样性、critic/reflection 和工具调用；每项必须证明正确率收益大于额外成本。

### A1：分层架构重建

优先迁回低风险资产：评测分包、原子 I/O、依赖锁、CI、secret/link 检查、许可证和确定性发布。随后迁入显式 `SolveContext`、统一 gateway、候选生命周期和工具子系统。物理包化作为最后一个结构变量单独验证。

退出条件：结构迁移前后离线行为等价，正式请求链不回退，所有活动模块只有一份实现，唯一架构文档与代码一致。

### Q2：统计证明能力提升

新版本相对恢复基线必须在冻结盲测上报告配对差异、置信区间、分领域结果、invalid、截断、请求和成本。只有重复实验支持的提升才能写入比赛材料；工程可维护性提升不能替代数学正确率证据。

2026-09-07 用户授权先完成 Q2 离线工程准备，R1 阶段和官方契约缺口保持待补。工程门槛须在模型运行前冻结：每组 3–10 次重复、至少 20 道独立题；均值正确率增量至少 2 个百分点、每次配对不退步，按题聚类的近似 percentile bootstrap 下界大于 0。开发集按预登记候选数进行 Bonferroni 调整，未知判分采用候选最不利情形；留出集只核验一个提前锁定的候选。不能把同一题的重复运行当作新增独立样本。

每次配对的不可用答案比例不得上升；请求、尝试、token 和耗时总量比例均不得超过 1.25，请求级截断率不得上升。阈值是本地预登记的工程决策，不是官方要求或效果承诺。模拟运行、缺失逐请求成本/截断证据、不完整矩阵、陈旧或变化的代码/配置/数据/输出一律不能晋级。近似置信区间不保证小样本或相关题目的精确覆盖率，也不保证赛事表现。

离线准备退出条件：预登记、重复统计、状态与指纹检查、候选锁定、留出使用约束、交付核验及反例通过完整门禁；正式运行时策略不因此改动。实际退出 Q2 还需要获授权的真实模型配对重复实验、冻结留出验证、Q0 双人盲审与发布/官方证据。自动统计 eligible 只是进入后续审核的资格，不是完整 Q2 能力验收。失败或中断运行保留原凭证与输出并停止该研究，不得删除后重新挑选有利批次；新研究必须披露前次失败，留出集已经使用则不得复用作独立验证。

## 6. 最低验收矩阵

| 变更 | 必须通过 | 停止条件 |
| --- | --- | --- |
| client/入口 | 官方预载、严格三参数、无私有读取、隔离导入、构造/solve 契约 | 任一请求前异常或额外关键字 |
| 模块拆分 | 导入闭包、污染矩阵、行为等价、唯一实现 | 通用裸模块名、隐式状态或第二实现 |
| 截断恢复 | 各阶段 length、恢复成功/失败/预算耗尽、唯一末行答案 | 残句进入聚合、恢复绕预算或 invalid 非零 |
| verifier/选择 | 正反例、数域、漏根/增根、冲突和 unknown | 不可证明结果被当成 pass |
| 题集/判分 | provenance、license、split、level、去重、四态判分、盲审 | 与 prompt 重合或字符串包含判对 |
| 发布 | 干净提交、离线全量、秘密扫描、依赖闭包、来源 SHA | 报告与提交不一致或缺少官方证据 |
| 规范与 Skill | `--paths` 事前触发、`--changed` 事后守卫、所有 Skill 覆盖层、规则 ID 一致性 | 未报告 blocker、规则清单漂移、Skill 绕过全局协议 |

## 7. 声明边界

- 离线门禁通过只说明已覆盖的确定性行为通过。
- 官方请求恢复只说明集成链恢复，不说明数学能力提高。
- 单次隐藏集分数只是一次观测；没有配对数据和重复不能证明显著变化。
- 包结构和工程拆分当前属于“可重新引入、尚待正式验证”，不得写成已证明安全或已证明导致零分。
- `OFFICIAL-GAP-*` 只能由对应的官方书面契约或绑定正式日志关闭。
