# 基于 Intern-S1 的数学智能体设计与推理创新 — 技术报告

## 一、系统概述

本系统基于官方 baseline（InternLM/Challenge-Cup-2026）增强，设计了一个具备题型路由、工具调用、验证纠错与答案聚合能力的数学推理智能体。系统遵循官方接口规范 `ReasoningAgent.solve(problem, metadata) -> {"final_response": str, "trace": [...]}`，在 generate-verify-select 基础架构上扩展了五大创新模块。

## 二、系统架构

### 2.1 五阶段流水线

```
题目输入
  │
  ▼
① 规划阶段（Planner）
  │  识别 18 子领域 → 获取领域专属 prompt → 生成求解策略
  ▼
② 候选生成阶段（Policy）
  │  工具增强候选×2（SymPy tool_calling） + 纯推理候选×1
  │  注入领域专属 prompt 引导解题方向
  ▼
③ 验证投票阶段（Verifier）
  │  独立验证器 ×2 轮投票，VERDICT: A/B 评估每个候选
  ▼
④ 反思纠错阶段（Reflection）
  │  最优候选验证不通过时，反馈错误信息，重试生成
  ▼
⑤ 答案聚合阶段（Aggregator）
  │  Self-consistency 多数投票 + 数值比较 + LaTeX 归一化
  ▼
最终答案输出
```

### 2.2 核心模块

| 模块 | 文件 | 职责 |
|------|------|------|
| 智能体入口 | user_agent.py | ReasoningAgent 类，五阶段流水线编排 |
| 数学工具 | math_tools.py | 6 个 SymPy 工具 + tool_calling 循环 |
| 领域路由 | domain_prompts.py | 18 子领域专属 prompt + 别名匹配 |
| LLM 客户端 | llm_client.py | InternChatClient（官方提供） |
| 本地运行器 | main.py | JSONL 读取、3 并发、断点续跑 |

## 三、创新点详述

### 3.1 题型识别与领域路由

**问题**：不同数学子领域（PDE、复分析、拓扑、运筹等）的解题方法差异巨大，通用 prompt 让模型自行摸索，难题容易走弯路。

**方案**：
- 规划阶段先用低温度（0.2）调用模型识别题目所属的 18 个子领域之一
- 为每个领域编写专属专家 prompt，包含三部分：
  - **典型解法**：如 PDE 提示"分离变量法"、复分析提示"留数公式"
  - **推荐工具**：如复分析提示用 `residue` 工具、微积分提示用 `integrate`
  - **易错点提醒**：如"有限域 F_{p^n} 元素个数是 p^n 不是 n"
- 通过别名映射（PDE→偏微分方程）和模糊匹配处理领域名称变体

**效果**：trace 中记录 `domain_routing` 步骤，直接支撑"系统设计"和"创新性"评分。样例 3 题领域识别全部正确。

### 3.2 工具增强求解

**问题**：大语言模型在复杂计算（求根、积分、留数）上容易出错，纯推理无法保证计算精度。

**方案**：
- 定义 6 个 SymPy 计算工具，遵循 OpenAI function calling 格式：

| 工具 | 功能 | 示例 |
|------|------|------|
| calculate | 化简/计算表达式 | 2\*\*10 → 1024 |
| solve_equation | 解方程 | x²-5x+6 → [2, 3] |
| differentiate | 求导 | x³-3x → 3x²-3 |
| integrate | 求积分 | 2x+1 → x²+x |
| limit | 求极限 | sin(x)/x→0 → 1 |
| residue | 求留数（复分析） | 1/((z-1)(z-2)²) at z=1 → 1 |

- 实现 `run_tool_loop` 循环：调用 client.chat 带 tools → 若返回 tool_calls 则本地执行 SymPy → 结果回灌 → 重复直到文本回复
- 最多 5 轮工具调用，防止死循环

**效果**：模型不再靠"心算"，而是调用 SymPy 精确计算，消灭算术错误。复分析题中模型主动调用 `residue` 工具计算留数。

### 3.3 反思纠错回路

**问题**：generate-verify-select 架构中，如果所有候选都错，验证器只能选出"最不错"的，无法纠正。

**方案**：
- 当最优候选的验证置信度 < 0.5 时，触发反思
- 将原题 + 候选解答 + 验证反馈组装为反思 prompt
- 用较低温度（0.3）重新生成候选，加入候选池参与后续聚合
- 反思候选同样经历验证投票，确保质量

**效果**：为难题提供"第二次机会"，trace 中记录 `reflection` 步骤。

### 3.4 答案归一化与数值比较

**问题**：模型输出答案格式不统一（72 vs 72.0 vs 72/1 vs \\frac{1}{8} vs -\\frac{1}{8}），导致 self-consistency 投票和 judger 匹配失败。

**方案**：
- **LaTeX 归一化**：\\frac{a}{b} → a/b，\\boxed{x} → x，去除 \\mathbb{}、$、\\left\\right 等
- **数值比较**：将答案转为 float 比较，72 == 72.0 == 72/1，消除格式差异
- **多策略抽取**：优先匹配"最终答案：XXX"，其次 \\boxed{}，其次"答案是"，最后末行

**效果**：样例中复分析题答案 -\\frac{1}{8} 成功归一化为 -1/8，匹配标准答案。

### 3.5 Self-consistency 聚合

**问题**：单候选正确率有限，验证器本身也可能误判。

**方案**：
- 生成 3 个候选（2 工具增强 + 1 纯推理），温度 0.6 增加多样性
- 答案级投票：多个候选给出相同答案（数值相等）时优先采用
- 若无一致答案，按验证置信度选最优
- 有清晰答案的候选加分（+0.3），无答案降权（-0.5）

**效果**：样例 3 题均有 ≥2 个候选答案一致，self-consistency 机制生效。

## 四、实验结果

### 4.1 样例数据测试

| idx | 科目 | 标准答案 | 智能体答案 | 正确 | 领域路由 | trace步数 |
|-----|------|---------|-----------|------|---------|----------|
| 0 | 抽象代数 | 72 | 72 | ✓ | 抽象代数 | 14 |
| 1 | 测度积分 | -1 | -1 | ✓ | 微积分 | 14 |
| 2 | 复分析 | -1/8 | -1/8 | ✓ | 复分析 | 14 |

**正确率：3/3 = 100%**

### 4.2 推理链质量

每题 trace 包含完整推理链：
- `plan`：题型识别与策略规划
- `domain_routing`：领域路由与专属 prompt 注入
- `tool_solve_*`：工具调用记录（工具名 + 结果）
- `policy_*`：候选生成记录
- `verify_*`：验证投票记录（VERDICT: A/B）
- `self_consistency`/`select_final`：答案聚合决策

### 4.3 优化历程

| 版本 | 正确率 | 关键改进 |
|------|--------|---------|
| v1（官方baseline增强） | 1/3 | 初版 generate-verify-select |
| v1 + 答案归一化 | 2/3 | LaTeX→纯文本，验证器关 thinking |
| v1 + self-consistency | 3/3 | 答案级多数投票 |
| v2（工具调用+反思） | 3/3 | SymPy tool_calling + 反思纠错 |
| v2 + 领域路由 | 3/3 | 18 子领域专属 prompt |

## 五、系统设计亮点

1. **不硬编码 API key**：完全依赖平台注入的 client，安全合规
2. **领域路由可扩展**：新增领域只需在 domain_prompts.py 添加条目
3. **工具系统可插拔**：math_tools.py 独立模块，新增工具只需注册到 TOOL_IMPLEMENTATIONS
4. **trace 完整可审计**：每步推理都有记录，支撑主观评分
5. **断点续跑**：main.py 支持已完成的题跳过，中断后可继续

## 六、依赖与环境

- Python 3.10+
- lagent（从 GitHub 源码安装：git+https://github.com/InternLM/lagent.git）
- sympy, requests, openai（详见 requirements.txt）
- 环境变量：INTERN_API_KEY（由平台管理，本地调试时自行配置）

## 七、未来工作

1. **增加候选数到 5 个**：提升 self-consistency 可靠性
2. **错误模式库**：跑批后归纳常见错误模式，加入 prompt 负例提醒
3. **决赛多智能体协作**：扩展为编排器+路由+4领域Solver+校验+解释的多Agent系统
4. **可交互 Demo**：Gradio Web 界面，流式展示推理过程
