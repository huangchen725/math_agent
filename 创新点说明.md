# 创新点说明 — 基于 Intern-S1 的数学智能体

> 题目编号：XH-202627 ｜ 发榜单位：上海人工智能实验室
> 本文档面向评审，逐条说明系统创新点，并映射到官方评分标准（创新性 10% + 推理策略 10%）。
> 当前实现边界与参数的唯一规范见 `ARCHITECTURE.md`；本文保留比赛陈述和实验解释用途。

---

## 0. 一句话定位

本系统在官方 baseline（`generate-verify-select` 单轮提示词架构）之上，构建了一个**「领域感知 + 有界调用 + 工具兜底 + 验证收敛」的推理智能体**。核心思想是在固定模型和显式调用边界下，把模型推理与本地确定性计算结合起来。

---

## 1. 创新点总览（映射评分标准）

| # | 创新点 | 状态 | 对应评分维度 | 核心价值 |
|---|---|---|---|---|
| 1 | 领域感知路由与题型分析 | ✅ 已实现 | 创新性 / 系统设计 | 18 子领域专属 prompt + 严格题型计划，降低模型“摸索成本” |
| 2 | 工具增强的符号计算 | ✅ 已实现 | 创新性 / 正确性 | 推理交给模型，计算交给 SymPy，消灭算术错误 |
| 3 | 证据驱动的验证与反思纠错 | ✅ 已实现 | 系统设计 / 正确性 | 严格题型确定性证据 + 同模型 verifier + 低置信度反思 |
| 4 | 答案归一化与等价比较 | ✅ 已实现 | 正确性 / 系统设计 | LaTeX/Unicode 归一化，消除"答对但格式不匹配"丢分 |
| 5 | 固定预算与有界回退 | ✅ 已实现 | 创新性 / 系统设计 | 候选数、工具轮数和回退条件显式受控 |
| 6 | 显式推理规划 + 多智能体协作 | 🔮 决赛扩展 | 创新性 / 可扩展性 | 子目标树 + 独立校验 Agent + 共享黑板 |

> **原则**：以上创新点均可通过代码、评测日志、trace 结构复现验证，无"花架子"。数据证据见第 3 节。

---

## 2. 创新点详述

### 创新点 1：领域感知的题型路由

**要解决的问题**：112 题覆盖 18 个数学子领域（PDE、复分析、拓扑、运筹…），解题方法差异巨大。通用 prompt 让模型在难题上"自己摸索"，容易走弯路、耗尽调用预算。

**创新做法**：为 18 个子领域分别编写**专家 prompt**，每个包含三要素——

| 要素 | 示例 |
|---|---|
| 典型解法 | 复分析 → 提示"留数公式 / 柯西积分定理" |
| 推荐工具 | 微积分 → 提示调用 `integrate`；复分析 → 提示 `residue` |
| 易错点提醒 | 有限域 → 提醒"F_{p^n} 元素个数是 p^n 不是 n" |

通过关键词匹配 + 别名映射（PDE→偏微分方程）实现**零 API 成本的本地路由**，不额外消耗调用预算。

**证据**：样例 3 题领域识别 100% 正确；`trace` 记录 `domain_detect` 步骤，直接支撑系统设计与创新性评分。

---

### 创新点 2：工具增强的符号计算

**要解决的问题**：大模型在复杂计算（求根、积分、留数、级数）上"心算"极易出错，这是数学题失分的头号原因。

**创新做法**：定义 11 个 SymPy 工具，覆盖表达式、方程、微积分、留数、矩阵、数论和组合计算，遵循 OpenAI function-calling 格式。**推理主体仍是 Intern 模型，但确定性计算交给受限符号引擎兜底**。

```
模型：判断"该用留数公式算这个积分"  ← 推理（Intern-S1）
工具：实际算出留数值 = 1/2            ← 计算（SymPy）
```

**证据**：复分析样题中模型主动调用 `residue` 工具精确算出留数，而非"心算"。

---

### 创新点 3：证据驱动的验证与反思纠错

**要解决的问题**：baseline 的 `generate-verify-select` 有个致命缺陷——**当所有候选都错时，验证器只能选出"最不错"的，无法纠错**。

**创新做法**：
1. **分层验证**：结构明确的直接题先产生确定性 `pass/fail/unknown` 证据，其余候选再由同模型 verifier 以独立提示视角给出 `VERDICT: A/B`；同模型 verifier 只提供辅助信号，不宣称独立真值；
2. **反思回路**：当最优候选置信度 < 0.5 时，将「原题 + 候选解答 + 验证反馈」组装为反思 prompt，用低温度（0.3）重生成，给难题"第二次机会"。

**证据**：`trace` 记录 `verify_*` 与 `reflection` 步骤；评测日志显示 invalid 输出从早期版本的 6 次降到 1 次。

---

### 创新点 4：答案归一化与等价比较

**要解决的问题**：模型输出答案格式不统一——`72` vs `72.0` vs `\frac{72}{1}`，`C_1` vs `C₁`，`ℤ` vs `Z`。这些"等价但不同形"的答案，会导致投票聚合和判分匹配失败。

**创新做法**：统一归一化管道——
- LaTeX 归一化：`\frac{a}{b}`→`a/b`、`\boxed{x}`→`x`、去 `\mathbb{}`/`\left\right`/`$`
- Unicode 归一化：下标 `₁₂`→`12`、`ℤℝℕℚℂ`→`ZRNQC`、`−×⋅≤≥≠`→`-*<=>=!=`
- 数值等价比较：`72 == 72.0 == 72/1`

**证据**：对照集 36 题中，3 处 judge 格式误判（`C₁` vs `C1`、`\frac` vs `/`、`ℤ` vs `Z`）经归一化后全部消解。

---

### 创新点 5：固定预算与有界回退

**要解决的问题**：缺少边界的工具循环、候选扩张和失败重试会放大调用成本，也会增加截断与超时风险。

**创新做法**：
- 默认固定为 2 个工具候选 + 1 个纯推理候选，避免无界扩张；
- 每个工具候选最多 3 轮，每轮最多执行 8 个工具调用；
- 只对低置信度最佳候选触发一次批评与反思；
- 候选截断或工具失败时进入纯推理/短答案回退，确保接口可收敛。

**历史证据**：08-07 → 08-09 两次评测中，调用次数从 1526 降到 563（-63%）、token 从 5.6M 降到 1.08M（-81%），正确率从 8.93% 上升到 11.61%。这些结果支持当前“显式限制调用”的方向，但不单独证明每个限额都是最优值。

---

### 创新点 6（决赛扩展）：显式推理规划 + 多智能体协作

**要解决的问题**：初赛单智能体在"超长程推理"（多轮推导 context 膨胀、路径死锁）上存在天花板。

**设计愿景**（可扩展性支撑，决赛落地）：
- **显式推理规划层**：解题前先生成"子目标树"，而非端到端直答，显著提升推理链可解释性；
- **独立校验 Agent**：校验与求解解耦，避免"自己查自己"的确认偏差，三层校验（语法/语义/结果）；
- **长程推理控制器**：状态压缩 + 子目标回溯 + 剪枝，解决 context 膨胀；
- **共享黑板架构**：Agent 间通过结构化黑板通信，新增 Agent 即插即用。

**当前边界**：多智能体尚未实现。现有运行时已统一到 `math_agent/` 包，根 `user_agent.py` 只保留竞赛兼容接口；长程规划和多智能体接入前仍需设计并验证新的内部协议。

---

## 3. 创新点的可验证性（数据证据汇总）

| 创新点 | 可复现证据 | 数据 |
|---|---|---|
| 领域/题型路由 | `math_agent/domain_prompts.py` + `math_agent/task_router.py` + trace | 本地完成，不增加模型调用 |
| 工具求解 | `math_agent/math_tools.py` 11 工具 + `trace.tool_solve_*` | 复分析题主动调 residue |
| 反思纠错 | `trace.reflection` | invalid 从 6 → 1 |
| 答案归一化 | `_normalize()` 单元测试 | 对照集 3 处误判消解 |
| 有界执行 | `AgentConfig` + 工具循环限制 | 固定候选、轮数、单轮调用数和回退条件 |
| 多 Agent 愿景 | 本文第 2 节 | 未来方向，当前未实现 |

---

## 4. 为什么这些创新点「有据」而非「花架子」

1. **每个创新点都有代码落点**：不是 PPT 概念，而是 `math_agent/` 唯一运行时包中的真实模块；
2. **每个创新点都有评测数据**：要么是样例正确率，要么是官方评测日志的 token/调用统计；
3. **失败案例也诚实记录**：技术报告 4.6 节记录了 4 次翻车（题级自适应、SC 多候选等），证明我们在**用数据驱动决策，而非堆砌炫技**——这本身就是"严谨工程"的体现。
